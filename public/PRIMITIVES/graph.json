{
  "primitive": "GRAPH_V2",
  "config": {
    "nodeRadius": 24,
    "colors": {
      "default": "rgba(255, 255, 255, 0.03)",
      "stroke": "#4f46e5",
      "accent": "#3b82f6",
      "highlight": "#eab308",
      "success": "#10b981",
      "ghost": "rgba(255, 255, 255, 0.015)",
      "ghostStroke": "rgba(79, 70, 229, 0.2)",
      "focusDim": "rgba(255, 255, 255, 0.008)",
      "focusDimStroke": "rgba(79, 70, 229, 0.12)",
      "edgeDefault": "rgba(255, 255, 255, 0.15)",
      "edgeGhost": "rgba(255, 255, 255, 0.05)"
    }
  },
  "functions": {
    "renderGraphBase": {
      "params": [
        "nodes",
        "edges",
        "type",
        "root"
      ],
      "code": "var colors = registry.config.colors; var nodeRadius = registry.config.nodeRadius; svg.selectAll('*').remove(); var width = svg.node().getBoundingClientRect().width || 500; var type = args.type || 'undirected'; var rootId = args.root || null; var nodeList = (args.nodes || []).map(function(n) { return { id: n.id, ghost: !!n.ghost }; }); var nodeMap = {}; nodeList.forEach(function(n) { nodeMap[n.id] = n; }); var edges = args.edges || []; var height = 250; var pad = nodeRadius + 20; var levelHeight = 80; if (type === 'binary-tree' || type === 'bst') { var childMap = {}; edges.forEach(function(e) { if (!childMap[e.from]) childMap[e.from] = {}; if (e.side === 'left') childMap[e.from].left = e.to; else if (e.side === 'right') childMap[e.from].right = e.to; else { if (!childMap[e.from].left) childMap[e.from].left = e.to; else childMap[e.from].right = e.to; } }); if (!rootId) { var hp = {}; edges.forEach(function(e) { hp[e.to] = true; }); for (var i = 0; i < nodeList.length; i++) { if (!hp[nodeList[i].id]) { rootId = nodeList[i].id; break; } } } var depthMap = {}; var maxDepth = 0; function computeDepth(nid, d) { if (!nid || !nodeMap[nid]) return; depthMap[nid] = d; if (d > maxDepth) maxDepth = d; var ch = childMap[nid] || {}; computeDepth(ch.left, d + 1); computeDepth(ch.right, d + 1); } computeDepth(rootId, 0); var inorderIdx = 0; var posMap = {}; function inorder(nid) { if (!nid || !nodeMap[nid]) return; var ch = childMap[nid] || {}; inorder(ch.left); posMap[nid] = inorderIdx++; inorder(ch.right); } inorder(rootId); var totalNodes = inorderIdx; levelHeight = Math.min(80, Math.max(50, (350 - 2 * pad) / Math.max(maxDepth, 1))); height = (maxDepth + 1) * levelHeight + 2 * pad; if (height < 180) height = 180; var availW = width - 2 * pad; nodeList.forEach(function(n) { if (posMap[n.id] !== undefined) { n.px = pad + (totalNodes <= 1 ? 0.5 : posMap[n.id] / (totalNodes - 1)) * availW; n.py = pad + depthMap[n.id] * levelHeight; n.depth = depthMap[n.id]; } else { n.px = width / 2; n.py = pad; n.depth = 0; } }); } else if (type === 'heap') { var nch = nodeList.length; var maxDepthH = nch > 0 ? Math.floor(Math.log2(nch)) : 0; levelHeight = Math.min(80, Math.max(50, (350 - 2 * pad) / Math.max(maxDepthH, 1))); height = (maxDepthH + 1) * levelHeight + 2 * pad; if (height < 180) height = 180; var availWH = width - 2 * pad; nodeList.forEach(function(node, idx) { var depth = Math.floor(Math.log2(idx + 1)); var posInLevel = idx + 1 - Math.pow(2, depth); var totalInLevel = Math.pow(2, depth); var spacing = availWH / totalInLevel; node.px = pad + spacing * (posInLevel + 0.5); node.py = pad + depth * levelHeight; node.depth = depth; }); } else if (type === 'cycle') { var ncyc = nodeList.length; var radius = ncyc <= 1 ? 0 : Math.min(width * 0.35, 160); var cx = width / 2; var cy = radius + pad; height = 2 * radius + 2 * pad; if (height < 180) height = 180; nodeList.forEach(function(node, idx) { if (ncyc <= 1) { node.px = cx; node.py = height / 2; return; } var angle = -Math.PI / 2 + (2 * Math.PI * idx) / ncyc; node.px = cx + radius * Math.cos(angle); node.py = cy + radius * Math.sin(angle); node.depth = 0; }); } else if (type === 'dag' || type === 'tree') { var g = new dagre.graphlib.Graph(); g.setGraph({ rankdir: 'TB', nodesep: 80, ranksep: 90 }); g.setDefaultEdgeLabel(function() { return {}; }); var ns = nodeRadius * 2 + 10; nodeList.forEach(function(n) { g.setNode(n.id, { width: ns, height: ns }); }); edges.forEach(function(e) { g.setEdge(e.from, e.to); }); dagre.layout(g); var minX = Infinity, maxX = -Infinity, minY = Infinity, maxY = -Infinity; nodeList.forEach(function(n) { var dn = g.node(n.id); if (dn.x < minX) minX = dn.x; if (dn.x > maxX) maxX = dn.x; if (dn.y < minY) minY = dn.y; if (dn.y > maxY) maxY = dn.y; }); var dw = (maxX - minX) || 1; var dh = (maxY - minY) || 1; var availWD = width - 2 * pad; var scale = Math.min(availWD / dw, 3); height = dh * scale + 2 * pad; if (height < 180) height = 180; var offX = pad + (availWD - dw * scale) / 2; var offY = pad + (height - 2 * pad - dh * scale) / 2; nodeList.forEach(function(n) { var dn = g.node(n.id); n.px = offX + (dn.x - minX) * scale; n.py = offY + (dn.y - minY) * scale; n.depth = 0; }); } else { height = Math.max(280, width * 0.55); var simLinks = edges.map(function(e) { return { source: e.from, target: e.to }; }); var chStr = nodeList.length <= 6 ? -400 : -250; var lnDist = nodeList.length <= 6 ? 120 : 90; var sim = d3.forceSimulation(nodeList).force('link', d3.forceLink(simLinks).id(function(d) { return d.id; }).distance(lnDist)).force('charge', d3.forceManyBody().strength(chStr)).force('center', d3.forceCenter(width / 2, height / 2)).force('collision', d3.forceCollide(nodeRadius + 20)).force('x', d3.forceX(width / 2).strength(0.06)).force('y', d3.forceY(height / 2).strength(0.06)).stop(); for (var fi = 0; fi < 300; fi++) sim.tick(); nodeList.forEach(function(n) { n.px = Math.max(pad, Math.min(width - pad, n.x)); n.py = Math.max(pad, Math.min(height - pad, n.y)); n.depth = 0; }); } svg.attr('data-graph-width', width).attr('data-graph-height', height).attr('data-graph-type', type).attr('data-graph-level-height', levelHeight).attr('data-graph-pad', pad); var defs = svg.append('defs'); var filter = defs.append('filter').attr('id', 'graph-glow').attr('x', '-50%').attr('y', '-50%').attr('width', '200%').attr('height', '200%'); filter.append('feGaussianBlur').attr('stdDeviation', '4').attr('result', 'coloredBlur'); var fm = filter.append('feMerge'); fm.append('feMergeNode').attr('in', 'coloredBlur'); fm.append('feMergeNode').attr('in', 'SourceGraphic'); var links = edges.map(function(e) { var fg = nodeMap[e.from] && nodeMap[e.from].ghost; var tg = nodeMap[e.to] && nodeMap[e.to].ghost; return { from: e.from, to: e.to, sx: nodeMap[e.from].px, sy: nodeMap[e.from].py, tx: nodeMap[e.to].px, ty: nodeMap[e.to].py, weight: e.weight, ghostEdge: !!(fg || tg) }; }); var edgesG = svg.append('g').attr('class', 'edges'); edgesG.selectAll('.edge').data(links).enter().append('line').attr('class', function(d) { return 'edge edge-' + d.from + '-' + d.to + ' edge-' + d.to + '-' + d.from; }).attr('x1', function(d) { return d.sx; }).attr('y1', function(d) { return d.sy; }).attr('x2', function(d) { return d.tx; }).attr('y2', function(d) { return d.ty; }).attr('stroke', function(d) { return d.ghostEdge ? colors.edgeGhost : colors.edgeDefault; }).attr('stroke-width', 2).style('stroke-linecap', 'round'); function edgeLabelX(d) { var dx = d.tx - d.sx; var dy = d.ty - d.sy; var len = Math.sqrt(dx * dx + dy * dy) || 1; return (d.sx + d.tx) / 2 + (-dy / len) * 8; } function edgeLabelY(d) { var dx = d.tx - d.sx; var dy = d.ty - d.sy; var len = Math.sqrt(dx * dx + dy * dy) || 1; return (d.sy + d.ty) / 2 + (dx / len) * 8; } edgesG.selectAll('.edge-weight').data(links.filter(function(d) { return d.weight !== undefined && d.weight !== null && d.weight !== ''; })).enter().append('text').attr('class', function(d) { return 'edge-weight edge-weight-' + d.from + '-' + d.to + ' edge-weight-' + d.to + '-' + d.from; }).attr('x', edgeLabelX).attr('y', edgeLabelY).attr('text-anchor', 'middle').attr('dominant-baseline', 'central').attr('fill', function(d) { return d.ghostEdge ? 'rgba(226,232,240,0.35)' : 'rgba(226,232,240,0.72)'; }).attr('data-default-fill', function(d) { return d.ghostEdge ? 'rgba(226,232,240,0.35)' : 'rgba(226,232,240,0.72)'; }).style('font-size', '10px').style('font-weight', '500').style('font-family', '\"Inter\", sans-serif').style('pointer-events', 'none').text(function(d) { return String(d.weight); }); var nodeG = svg.append('g').attr('class', 'nodes').selectAll('.node-grp').data(nodeList).enter().append('g').attr('class', 'node-grp').attr('id', function(d) { return 'node-' + d.id; }).attr('data-depth', function(d) { return d.depth || 0; }).attr('data-ghost', function(d) { return d.ghost ? 'true' : null; }).attr('transform', function(d) { return 'translate(' + d.px + ',' + d.py + ')'; }); nodeG.append('circle').attr('r', nodeRadius).attr('fill', function(d) { return d.ghost ? colors.ghost : colors.default; }).attr('stroke', function(d) { return d.ghost ? colors.ghostStroke : colors.stroke; }).attr('stroke-width', 2).attr('stroke-dasharray', function(d) { return d.ghost ? '4,3' : 'none'; }); nodeG.append('text').text(function(d) { return d.id; }).attr('text-anchor', 'middle').attr('dominant-baseline', 'central').attr('fill', function(d) { return d.ghost ? 'rgba(255,255,255,0.2)' : '#ffffff'; }).style('font-weight', '600').style('font-size', '14px').style('font-family', '\"Inter\", sans-serif').style('pointer-events', 'none');"
    },
    "addNode": {
      "params": [
        "id",
        "parent",
        "side"
      ],
      "code": "var colors = registry.config.colors; var nodeRadius = registry.config.nodeRadius; var width = +svg.attr('data-graph-width') || svg.node().getBoundingClientRect().width || 500; var pad = +svg.attr('data-graph-pad') || nodeRadius + 20; var type = svg.attr('data-graph-type') || 'undirected'; var levelHeight = +svg.attr('data-graph-level-height') || 80; var px, py, depth = 0; if (args.parent) { var parentG = svg.select('#node-' + args.parent); if (!parentG.empty()) { var tr = parentG.attr('transform'); var m = tr.match(/translate\\(([^,]+),([^)]+)\\)/); var parentX = m ? +m[1] : width / 2; var parentY = m ? +m[2] : pad; var parentDepth = +parentG.attr('data-depth') || 0; depth = parentDepth + 1; py = parentY + levelHeight; var availW = width - 2 * pad; var spread = availW / Math.pow(2, depth + 1); spread = Math.max(nodeRadius * 2.5, spread); if (args.side === 'left') { px = parentX - spread; } else { px = parentX + spread; } } else { px = width / 2; py = pad; } } else { px = width / 2; py = pad + 40; } px = Math.max(pad, Math.min(width - pad, px)); var nodesG = svg.select('.nodes'); if (nodesG.empty()) nodesG = svg.append('g').attr('class', 'nodes'); var nodeG = nodesG.append('g').attr('class', 'node-grp').attr('id', 'node-' + args.id).attr('data-depth', depth).attr('transform', 'translate(' + px + ',' + py + ')').style('opacity', 0); nodeG.append('circle').attr('r', 0).attr('fill', colors.default).attr('stroke', colors.stroke).attr('stroke-width', 2); nodeG.append('text').text(args.id).attr('text-anchor', 'middle').attr('dominant-baseline', 'central').attr('fill', '#ffffff').style('font-weight', '600').style('font-size', '14px').style('font-family', '\"Inter\", sans-serif').style('pointer-events', 'none'); nodeG.transition().duration(400).style('opacity', 1); nodeG.select('circle').transition().duration(400).attr('r', nodeRadius); if (args.parent) { var srcG = svg.select('#node-' + args.parent); if (!srcG.empty()) { var srcTr = srcG.attr('transform'); var sm = srcTr.match(/translate\\(([^,]+),([^)]+)\\)/); if (sm) { var edgesG = svg.select('.edges'); if (edgesG.empty()) edgesG = svg.insert('g', '.nodes').attr('class', 'edges'); var line = edgesG.append('line').attr('class', 'edge edge-' + args.parent + '-' + args.id + ' edge-' + args.id + '-' + args.parent).attr('x1', +sm[1]).attr('y1', +sm[2]).attr('x2', +sm[1]).attr('y2', +sm[2]).attr('stroke', colors.edgeDefault).attr('stroke-width', 2).style('stroke-linecap', 'round'); line.transition().duration(400).attr('x2', px).attr('y2', py); } } } var currentHeight = +svg.attr('data-graph-height') || 250; var neededHeight = py + pad + 12; if (neededHeight > currentHeight) { svg.attr('data-graph-height', neededHeight); }"
    },
    "addEdge": {
      "params": [
        "from",
        "to"
      ],
      "code": "var colors = registry.config.colors; function getPos(nodeId) { var grp = svg.select('#node-' + nodeId); if (grp.empty()) return null; var tr = grp.attr('transform'); var m = tr.match(/translate\\(([^,]+),([^)]+)\\)/); return m ? { px: +m[1], py: +m[2] } : null; } var src = getPos(args.from); var tgt = getPos(args.to); if (!src || !tgt) return; var edgesG = svg.select('.edges'); if (edgesG.empty()) edgesG = svg.insert('g', '.nodes').attr('class', 'edges'); var line = edgesG.append('line').attr('class', 'edge edge-' + args.from + '-' + args.to + ' edge-' + args.to + '-' + args.from).attr('x1', src.px).attr('y1', src.py).attr('x2', src.px).attr('y2', src.py).attr('stroke', colors.edgeDefault).attr('stroke-width', 2).style('stroke-linecap', 'round'); line.transition().duration(400).attr('x2', tgt.px).attr('y2', tgt.py); if (args.weight !== undefined && args.weight !== null && args.weight !== '') { var dx = tgt.px - src.px; var dy = tgt.py - src.py; var len = Math.sqrt(dx * dx + dy * dy) || 1; var lx = (src.px + tgt.px) / 2 + (-dy / len) * 8; var ly = (src.py + tgt.py) / 2 + (dx / len) * 8; var baseFill = 'rgba(226,232,240,0.72)'; var label = edgesG.append('text').attr('class', 'edge-weight edge-weight-' + args.from + '-' + args.to + ' edge-weight-' + args.to + '-' + args.from).attr('x', src.px).attr('y', src.py).attr('text-anchor', 'middle').attr('dominant-baseline', 'central').attr('fill', baseFill).attr('data-default-fill', baseFill).style('font-size', '10px').style('font-weight', '500').style('font-family', '\"Inter\", sans-serif').style('pointer-events', 'none').style('opacity', 0).text(String(args.weight)); label.transition().duration(400).attr('x', lx).attr('y', ly).style('opacity', 1); }"
    },
    "setNodeAnnotation": {
      "params": [
        "nodeId",
        "text"
      ],
      "code": "var nodeRadius = registry.config.nodeRadius; var nodeG = svg.select('#node-' + args.nodeId); if (nodeG.empty()) return; nodeG.select('.node-annotation').remove(); if (!args.text) return; nodeG.append('text').attr('class', 'node-annotation').text(args.text).attr('text-anchor', 'middle').attr('y', nodeRadius + 14).attr('fill', '#94a3b8').style('font-size', '11px').style('font-family', '\"Inter\", sans-serif').style('pointer-events', 'none').style('opacity', 0).transition().duration(300).style('opacity', 1);"
    },
    "setNodeHighlight": {
      "params": [
        "nodeId",
        "active"
      ],
      "code": "var colors = registry.config.colors; var grp = svg.select('#node-' + args.nodeId); if (grp.empty()) return; var circle = grp.select('circle'); if (circle.empty()) return; var isGhost = grp.attr('data-ghost') === 'true'; if (args.active) { circle.transition().duration(300).attr('stroke', colors.accent).attr('stroke-width', 3).attr('fill', 'rgba(59,130,246,0.15)').attr('stroke-dasharray', 'none').style('filter', 'url(#graph-glow)'); } else { circle.transition().duration(300).attr('stroke', isGhost ? colors.ghostStroke : colors.stroke).attr('stroke-width', 2).attr('fill', isGhost ? colors.ghost : colors.default).attr('stroke-dasharray', isGhost ? '4,3' : 'none').style('filter', 'none'); }"
    },
    "setEdgeHighlight": {
      "params": [
        "from",
        "to",
        "active"
      ],
      "code": "var colors = registry.config.colors; var edge = svg.select('.edge-' + args.from + '-' + args.to); if (edge.empty()) edge = svg.select('.edge-' + args.to + '-' + args.from); if (edge.empty()) return; edge.transition().duration(300).attr('stroke', args.active ? colors.accent : colors.edgeDefault).attr('stroke-width', args.active ? 4 : 2).style('filter', args.active ? 'url(#graph-glow)' : 'none'); var label = svg.select('.edge-weight-' + args.from + '-' + args.to); if (label.empty()) label = svg.select('.edge-weight-' + args.to + '-' + args.from); if (!label.empty()) { label.transition().duration(300).attr('fill', args.active ? 'rgba(226,232,240,0.95)' : function() { return d3.select(this).attr('data-default-fill') || 'rgba(226,232,240,0.72)'; }); }"
    },
    "setFocus": {
      "params": [
        "nodeIds"
      ],
      "code": "var colors = registry.config.colors; var focusIds = args.nodeIds || []; if (focusIds.length === 0) { svg.selectAll('.node-grp').each(function() { var grp = d3.select(this); var isGhost = grp.attr('data-ghost') === 'true'; grp.select('circle').transition().duration(300).attr('fill', isGhost ? colors.ghost : colors.default).attr('stroke', isGhost ? colors.ghostStroke : colors.stroke).attr('stroke-width', 2).attr('stroke-dasharray', isGhost ? '4,3' : 'none').style('filter', 'none'); grp.select('text').transition().duration(300).attr('fill', isGhost ? 'rgba(255,255,255,0.2)' : '#ffffff'); }); svg.selectAll('.edge').transition().duration(300).attr('stroke', colors.edgeDefault).attr('stroke-width', 2).style('filter', 'none'); svg.selectAll('.edge-weight').transition().duration(300).attr('fill', function() { return d3.select(this).attr('data-default-fill') || 'rgba(226,232,240,0.72)'; }); return; } svg.selectAll('.node-grp').each(function() { var grp = d3.select(this); var nid = grp.attr('id').replace('node-', ''); var isFocused = focusIds.indexOf(nid) >= 0; grp.select('circle').transition().duration(400).attr('fill', isFocused ? colors.default : colors.focusDim).attr('stroke', isFocused ? colors.stroke : colors.focusDimStroke).attr('stroke-width', isFocused ? 2 : 1).attr('stroke-dasharray', 'none').style('filter', 'none'); grp.select('text').transition().duration(400).attr('fill', isFocused ? '#ffffff' : 'rgba(255,255,255,0.12)'); }); svg.selectAll('.edge').each(function() { var el = d3.select(this); var cls = el.attr('class') || ''; var connected = false; for (var i = 0; i < focusIds.length && !connected; i++) { for (var j = 0; j < focusIds.length && !connected; j++) { if (cls.indexOf('edge-' + focusIds[i] + '-' + focusIds[j]) >= 0) connected = true; } } el.transition().duration(400).attr('stroke', connected ? colors.edgeDefault : 'rgba(255,255,255,0.04)').attr('stroke-width', connected ? 2 : 1); }); svg.selectAll('.edge-weight').each(function() { var el = d3.select(this); var cls = el.attr('class') || ''; var connected = false; for (var i = 0; i < focusIds.length && !connected; i++) { for (var j = 0; j < focusIds.length && !connected; j++) { if (cls.indexOf('edge-weight-' + focusIds[i] + '-' + focusIds[j]) >= 0) connected = true; } } el.transition().duration(400).attr('fill', connected ? (el.attr('data-default-fill') || 'rgba(226,232,240,0.72)') : 'rgba(226,232,240,0.22)'); });"
    },
    "setGhost": {
      "params": [
        "nodeIds",
        "active"
      ],
      "code": "var colors = registry.config.colors; var nodeIds = args.nodeIds || []; var active = args.active !== false; nodeIds.forEach(function(nid) { var grp = svg.select('#node-' + nid); if (grp.empty()) return; grp.attr('data-ghost', active ? 'true' : null); grp.select('circle').transition().duration(400).attr('fill', active ? colors.ghost : colors.default).attr('stroke', active ? colors.ghostStroke : colors.stroke).attr('stroke-dasharray', active ? '4,3' : 'none'); grp.select('text').transition().duration(400).attr('fill', active ? 'rgba(255,255,255,0.2)' : '#ffffff'); });"
    },
    "clearHighlights": {
      "params": [],
      "code": "var colors = registry.config.colors; svg.selectAll('.node-grp').each(function() { var grp = d3.select(this); var isGhost = grp.attr('data-ghost') === 'true'; grp.select('circle').transition().duration(300).attr('stroke', isGhost ? colors.ghostStroke : colors.stroke).attr('stroke-width', 2).attr('fill', isGhost ? colors.ghost : colors.default).attr('stroke-dasharray', isGhost ? '4,3' : 'none').style('filter', 'none'); grp.select('text').transition().duration(300).attr('fill', isGhost ? 'rgba(255,255,255,0.2)' : '#ffffff'); }); svg.selectAll('.edge').transition().duration(300).attr('stroke', colors.edgeDefault).attr('stroke-width', 2).style('filter', 'none'); svg.selectAll('.edge-weight').transition().duration(300).attr('fill', function() { return d3.select(this).attr('data-default-fill') || 'rgba(226,232,240,0.72)'; }).style('opacity', 1); svg.selectAll('.node-annotation').transition().duration(300).style('opacity', 0).remove();"
    }
  }
}
