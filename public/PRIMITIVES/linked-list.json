{
  "primitive": "LINKED_LIST_V1",
  "config": {
    "nodeW": 90,
    "nodeH": 54,
    "addrH": 20,
    "gap": 60,
    "baseY": 60,
    "colors": {
      "default": "transparent",
      "stroke": "#4f46e5",
      "text": "#ffffff",
      "addrText": "#94a3b8",
      "addrBg": "rgba(79,70,229,0.10)",
      "highlight": "#eab308",
      "active": "#6366f1",
      "success": "#10b981",
      "dull": "rgba(255,255,255,0.04)",
      "pointer": "#f43f5e",
      "link": "#4f46e5",
      "linkArrow": "#818cf8",
      "null": "#475569"
    }
  },
  "functions": {
    "renderLinkedListBase": {
      "params": [
        "nodes"
      ],
      "description": "Renders the initial linked list. nodes = [{value, addr}] in order.",
      "code": "svg.selectAll('.ll-node,.ll-link,.ll-null,.ll-annotation,defs').remove(); const { nodeW, nodeH, addrH, gap, baseY, colors } = registry.config; const svgW = svg.node().getBoundingClientRect().width || 700; const n = args.nodes.length; const totalW = n * nodeW + (n - 1) * gap + (n > 0 ? 60 : 0); let startX = Math.max(16, (svgW - totalW) / 2); svg.attr('data-ll-startx', startX).attr('data-ll-n', n); const defs = svg.append('defs'); const marker = defs.append('marker').attr('id','ll-arrow').attr('markerWidth',10).attr('markerHeight',7).attr('refX',9).attr('refY',3.5).attr('orient','auto'); marker.append('polygon').attr('points','0 0, 10 3.5, 0 7').attr('fill', colors.linkArrow); const filter = defs.append('filter').attr('id','ll-glow'); filter.append('feGaussianBlur').attr('stdDeviation','3').attr('result','coloredBlur'); const merge = filter.append('feMerge'); merge.append('feMergeNode').attr('in','coloredBlur'); merge.append('feMergeNode').attr('in','SourceGraphic'); const nodesData = args.nodes.map((nd, i) => ({ ...nd, idx: i })); svg.attr('data-ll-nodes', JSON.stringify(nodesData)); const groups = svg.selectAll('.ll-node').data(nodesData, d => d.idx).enter().append('g').attr('class','ll-node').attr('id', d => `ll-node-${d.idx}`).attr('transform', (d,i) => `translate(${startX + i*(nodeW+gap)}, ${baseY - 16})`).style('opacity', 0); groups.append('rect').attr('class','ll-val-box').attr('width', nodeW * 0.55).attr('height', nodeH).attr('fill', colors.default).attr('stroke', colors.stroke).attr('stroke-width', 2).attr('rx', 4); groups.append('rect').attr('class','ll-next-box').attr('x', nodeW * 0.55).attr('width', nodeW * 0.45).attr('height', nodeH).attr('fill','rgba(79,70,229,0.08)').attr('stroke', colors.stroke).attr('stroke-width', 2).attr('rx', 0); groups.append('text').attr('class','ll-val-text').text(d => d.value).attr('x', nodeW * 0.275).attr('y', nodeH/2 + 5).attr('text-anchor','middle').attr('fill', colors.text).style('font-size','16px').style('font-weight','600').style('font-family','Inter,sans-serif'); groups.append('text').attr('class','ll-next-label').text((d,i) => i < nodesData.length-1 ? nodesData[i+1].addr : 'NULL').attr('x', nodeW * 0.55 + nodeW * 0.225).attr('y', nodeH/2 + 5).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','9px').style('font-family','ui-monospace,monospace'); groups.append('rect').attr('class','ll-addr-box').attr('y', nodeH + 4).attr('width', nodeW).attr('height', addrH).attr('fill', colors.addrBg).attr('rx', 3); groups.append('text').attr('class','ll-addr-text').text(d => d.addr).attr('x', nodeW/2).attr('y', nodeH + 4 + addrH/2 + 4).attr('text-anchor','middle').attr('fill', colors.addrText).style('font-size','10px').style('font-family','ui-monospace,monospace'); groups.transition().delay((d,i) => i * 80).duration(520).ease(d3.easeBackOut.overshoot(1.1)).attr('transform', (d,i) => `translate(${startX + i*(nodeW+gap)}, ${baseY})`).style('opacity', 1); for (let i = 0; i < nodesData.length - 1; i++) { const x1 = startX + i*(nodeW+gap) + nodeW; const x2 = startX + (i+1)*(nodeW+gap); const y = baseY + nodeH/2; const len = Math.max(1, x2 - x1 - 2); svg.append('line').attr('class','ll-link').attr('id',`ll-link-${i}`).attr('x1', x1).attr('y1', y).attr('x2', x2 - 2).attr('y2', y).attr('stroke', colors.link).attr('stroke-width', 2).attr('marker-end','url(#ll-arrow)').attr('stroke-dasharray', len).attr('stroke-dashoffset', len).transition().delay(i*80 + 420).duration(480).ease(d3.easeCubicInOut).attr('stroke-dashoffset', 0); } if (nodesData.length > 0) { const lastX = startX + (nodesData.length-1)*(nodeW+gap) + nodeW; svg.append('g').attr('class','ll-null').attr('transform',`translate(${lastX + 12}, ${baseY + nodeH/2 - 10})`).style('opacity',0).call(g => { g.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke', colors.null).attr('stroke-width',1.5); g.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill', colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace'); }).transition().delay(nodesData.length*80 + 380).duration(420).ease(d3.easeCubicInOut).style('opacity',1); }"
    },
    "highlightNode": {
      "params": [
        "index",
        "active"
      ],
      "description": "Highlights a node by index. active=true to highlight, false to reset.",
      "code": "const { colors } = registry.config; const node = svg.select(`#ll-node-${args.index}`); if (node.empty()) return; node.select('.ll-val-box').transition().duration(420).ease(d3.easeCubicInOut).attr('stroke', args.active ? colors.highlight : colors.stroke).attr('stroke-width', args.active ? 3 : 2).style('filter', args.active ? 'url(#ll-glow)' : 'none'); node.select('.ll-val-text').transition().duration(420).ease(d3.easeCubicInOut).attr('fill', args.active ? colors.highlight : colors.text);"
    },
    "highlightLink": {
      "params": [
        "index",
        "active"
      ],
      "description": "Highlights the link from node[index] to node[index+1].",
      "code": "const { colors } = registry.config; const link = svg.select(`#ll-link-${args.index}`); if (link.empty()) return; link.transition().duration(420).ease(d3.easeCubicInOut).attr('stroke', args.active ? colors.highlight : colors.link).attr('stroke-width', args.active ? 3 : 2);"
    },
    "addNode": {
      "params": [
        "value",
        "addr",
        "atIndex"
      ],
      "description": "Inserts a new node at atIndex (0 = head). Redraws entire list from stored data.",
      "code": "const { nodeW, nodeH, addrH, gap, baseY, colors } = registry.config; let nodesData = JSON.parse(svg.attr('data-ll-nodes') || '[]'); const newNode = { value: args.value, addr: args.addr }; const idx = args.atIndex === undefined ? nodesData.length : Math.max(0, Math.min(args.atIndex, nodesData.length)); nodesData.splice(idx, 0, newNode); nodesData = nodesData.map((nd, i) => ({ ...nd, idx: i })); svg.attr('data-ll-nodes', JSON.stringify(nodesData)).attr('data-ll-n', nodesData.length); svg.selectAll('.ll-node,.ll-link,.ll-null,.ll-annotation').remove(); const svgW = svg.node().getBoundingClientRect().width || 700; const totalW = nodesData.length * nodeW + (nodesData.length-1) * gap + 60; let startX = Math.max(16, (svgW - totalW) / 2); svg.attr('data-ll-startx', startX); const groups = svg.selectAll('.ll-node').data(nodesData, d => d.idx).enter().append('g').attr('class','ll-node').attr('id', d => `ll-node-${d.idx}`).attr('transform', (d,i) => i === idx ? `translate(${startX + i*(nodeW+gap)}, ${baseY - 20})` : `translate(${startX + i*(nodeW+gap)}, ${baseY})`).style('opacity', (d,i) => i === idx ? 0 : 1); groups.append('rect').attr('class','ll-val-box').attr('width', nodeW*0.55).attr('height', nodeH).attr('fill', colors.default).attr('stroke', d => d.idx === idx ? colors.success : colors.stroke).attr('stroke-width',2).attr('rx',4); groups.append('rect').attr('class','ll-next-box').attr('x', nodeW*0.55).attr('width', nodeW*0.45).attr('height', nodeH).attr('fill','rgba(79,70,229,0.08)').attr('stroke', d => d.idx === idx ? colors.success : colors.stroke).attr('stroke-width',2).attr('rx',0); groups.append('text').attr('class','ll-val-text').text(d => d.value).attr('x', nodeW*0.275).attr('y', nodeH/2+5).attr('text-anchor','middle').attr('fill', colors.text).style('font-size','16px').style('font-weight','600').style('font-family','Inter,sans-serif'); groups.append('text').attr('class','ll-next-label').text((d,i) => i < nodesData.length-1 ? nodesData[i+1].addr : 'NULL').attr('x', nodeW*0.55+nodeW*0.225).attr('y', nodeH/2+5).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','9px').style('font-family','ui-monospace,monospace'); groups.append('rect').attr('class','ll-addr-box').attr('y', nodeH+4).attr('width', nodeW).attr('height', addrH).attr('fill', colors.addrBg).attr('rx',3); groups.append('text').attr('class','ll-addr-text').text(d => d.addr).attr('x', nodeW/2).attr('y', nodeH+4+addrH/2+4).attr('text-anchor','middle').attr('fill', colors.addrText).style('font-size','10px').style('font-family','ui-monospace,monospace'); svg.select(`#ll-node-${idx}`).transition().delay(80).duration(520).ease(d3.easeBackOut.overshoot(1.15)).attr('transform', `translate(${startX + idx*(nodeW+gap)}, ${baseY})`).style('opacity', 1); for (let i = 0; i < nodesData.length-1; i++) { const x1 = startX + i*(nodeW+gap) + nodeW; const x2 = startX + (i+1)*(nodeW+gap); const y = baseY + nodeH/2; const len = Math.max(1, x2 - x1 - 2); svg.append('line').attr('class','ll-link').attr('id',`ll-link-${i}`).attr('x1', x1).attr('y1', y).attr('x2', x2-2).attr('y2', y).attr('stroke', i === idx-1 || i === idx ? colors.success : colors.link).attr('stroke-width',2).attr('marker-end','url(#ll-arrow)').attr('stroke-dasharray', len).attr('stroke-dashoffset', len).transition().delay(i === idx-1 || i === idx ? 480 : 0).duration(460).ease(d3.easeCubicInOut).attr('stroke-dashoffset', 0).transition().duration(400).attr('stroke', colors.link); } if (nodesData.length > 0) { const lastX = startX + (nodesData.length-1)*(nodeW+gap) + nodeW; svg.append('g').attr('class','ll-null').attr('transform',`translate(${lastX+12}, ${baseY+nodeH/2-10})`).style('opacity',0).call(g => { g.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5); g.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace'); }).transition().delay(420).duration(380).ease(d3.easeCubicInOut).style('opacity',1); } svg.selectAll('.ll-pointer').each(function() { const ptrEl = d3.select(this); let ptrIdx = parseInt(ptrEl.attr('data-ptr-index')); if (isNaN(ptrIdx)) return; if (ptrIdx >= idx) ptrIdx += 1; ptrEl.attr('data-ptr-index', ptrIdx); const newCx = startX + ptrIdx*(nodeW+gap) + nodeW*0.275; ptrEl.transition().duration(520).ease(d3.easeCubicInOut).attr('transform', `translate(${newCx}, ${baseY-4})`); });"
    },
    "deleteNode": {
      "params": [
        "index"
      ],
      "description": "Fades the target node up and out, then slides remaining nodes to fill the gap.",
      "code": "const { nodeW, nodeH, addrH, gap, baseY, colors } = registry.config; let nodesData = JSON.parse(svg.attr('data-ll-nodes') || '[]'); if (args.index < 0 || args.index >= nodesData.length) return; const svgW = svg.node().getBoundingClientRect().width || 700; const target = svg.select(`#ll-node-${args.index}`); const currentTransform = target.attr('transform') || ''; const txMatch = currentTransform.match(/translate\\(([^,]+)/); const tx = txMatch ? parseFloat(txMatch[1]) : 0; svg.select(`#ll-link-${args.index - 1}`).transition().duration(320).ease(d3.easeCubicInOut).style('opacity', 0); svg.select(`#ll-link-${args.index}`).transition().duration(320).ease(d3.easeCubicInOut).style('opacity', 0); target.select('.ll-val-box').transition().duration(260).ease(d3.easeCubicOut).attr('stroke', '#ef4444').attr('stroke-width', 3).transition().duration(60).ease(d3.easeCubicIn).attr('stroke-width', 2); target.transition().delay(200).duration(460).ease(d3.easeCubicIn).attr('transform', `translate(${tx}, ${baseY - 26})`).style('opacity', 0).on('end', function() { d3.select(this).remove(); svg.selectAll('.ll-link, .ll-null').remove(); nodesData.splice(args.index, 1); nodesData = nodesData.map((nd, i) => ({ ...nd, idx: i })); svg.attr('data-ll-nodes', JSON.stringify(nodesData)).attr('data-ll-n', nodesData.length); const newTotalW = nodesData.length * nodeW + Math.max(0, nodesData.length - 1) * gap + 60; const newStartX = Math.max(16, (svgW - newTotalW) / 2); svg.attr('data-ll-startx', newStartX); svg.selectAll('.ll-node').each(function() { const el = d3.select(this); const oldId = parseInt((el.attr('id') || '').replace('ll-node-', '')); const newIdx = oldId > args.index ? oldId - 1 : oldId; el.attr('id', `ll-node-${newIdx}`); const nextAddr = newIdx < nodesData.length - 1 ? nodesData[newIdx + 1].addr : 'NULL'; el.select('.ll-next-label').text(nextAddr); el.transition().duration(520).ease(d3.easeCubicInOut).attr('transform', `translate(${newStartX + newIdx*(nodeW+gap)}, ${baseY})`); }); svg.selectAll('.ll-pointer').each(function() { const ptrEl = d3.select(this); let ptrIdx = parseInt(ptrEl.attr('data-ptr-index')); if (isNaN(ptrIdx)) return; if (ptrIdx === args.index) { ptrIdx = Math.min(ptrIdx, nodesData.length - 1); } else if (ptrIdx > args.index) { ptrIdx -= 1; } if (ptrIdx < 0 || nodesData.length === 0) { ptrEl.transition().duration(380).style('opacity', 0).remove(); return; } ptrEl.attr('data-ptr-index', ptrIdx); const newCx = newStartX + ptrIdx*(nodeW+gap) + nodeW*0.275; ptrEl.transition().duration(520).ease(d3.easeCubicInOut).attr('transform', `translate(${newCx}, ${baseY-4})`); }); const drawLinks = () => { for (let i = 0; i < nodesData.length - 1; i++) { const x1 = newStartX + i*(nodeW+gap) + nodeW; const x2 = newStartX + (i+1)*(nodeW+gap); const y = baseY + nodeH/2; const len = Math.max(1, x2 - x1 - 2); svg.append('line').attr('class','ll-link').attr('id',`ll-link-${i}`).attr('x1',x1).attr('y1',y).attr('x2',x2-2).attr('y2',y).attr('stroke',colors.link).attr('stroke-width',2).attr('marker-end','url(#ll-arrow)').attr('stroke-dasharray', len).attr('stroke-dashoffset', len).transition().delay(i*55).duration(440).ease(d3.easeCubicInOut).attr('stroke-dashoffset', 0); } if (nodesData.length > 0) { const lastX = newStartX + (nodesData.length-1)*(nodeW+gap) + nodeW; svg.append('g').attr('class','ll-null').attr('transform',`translate(${lastX+12}, ${baseY+nodeH/2-10})`).style('opacity',0).call(g => { g.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5); g.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace'); }).transition().delay(nodesData.length*55).duration(380).ease(d3.easeCubicInOut).style('opacity',1); } }; setTimeout(drawLinks, 260); });"
    },
    "movePointer": {
      "params": [
        "index",
        "label"
      ],
      "description": "Places a named pointer arrow above the node at index.",
      "code": "const { nodeW, nodeH, gap, baseY, colors } = registry.config; const svgW = svg.node().getBoundingClientRect().width || 700; const nodesData = JSON.parse(svg.attr('data-ll-nodes') || '[]'); const n = nodesData.length; const totalW = n*nodeW+(n-1)*gap+60; const startX = parseFloat(svg.attr('data-ll-startx')) || Math.max(16,(svgW-totalW)/2); const cx = startX + args.index*(nodeW+gap) + nodeW*0.275; const label = args.label || 'ptr'; let ptr = svg.select(`.ll-ptr-${label}`); if (ptr.empty()) { ptr = svg.append('g').attr('class',`ll-pointer ll-ptr-${label}`).style('opacity',0).attr('transform',`translate(${cx}, ${baseY - 18})`); ptr.append('path').attr('d','M -7 -14 L 0 -4 L 7 -14 Z').attr('fill', colors.pointer); ptr.append('text').attr('y',-18).attr('text-anchor','middle').attr('fill',colors.pointer).style('font-weight','bold').style('font-size','12px').style('font-family','Inter,sans-serif').text(label); ptr.transition().duration(380).ease(d3.easeCubicOut).style('opacity',1).attr('transform',`translate(${cx}, ${baseY-4})`); } else { ptr.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${cx}, ${baseY-4})`); } ptr.attr('data-ptr-index', args.index);"
    },
    "setNodeDull": {
      "params": [
        "index",
        "isDull"
      ],
      "description": "Dims or restores a node.",
      "code": "svg.select(`#ll-node-${args.index}`).transition().duration(500).ease(d3.easeCubicInOut).style('opacity', args.isDull ? 0.18 : 1).style('filter', args.isDull ? 'grayscale(80%) blur(0.8px)' : 'none');"
    },
    "updateValue": {
      "params": [
        "index",
        "value"
      ],
      "description": "Updates the value label of a node in place with a flash animation.",
      "code": "const { nodeH, nodeW, colors } = registry.config; const node = svg.select(`#ll-node-${args.index}`); if (node.empty()) return; node.select('.ll-val-box').transition().duration(260).ease(d3.easeCubicOut).attr('fill','rgba(99,102,241,0.35)').transition().duration(360).ease(d3.easeCubicInOut).attr('fill','transparent'); node.select('.ll-val-text').transition().duration(220).ease(d3.easeCubicOut).attr('fill', colors.active).on('end', function() { d3.select(this).text(args.value).transition().duration(380).ease(d3.easeCubicInOut).attr('fill', colors.text); });"
    },
    "annotate": {
      "params": [
        "index",
        "text"
      ],
      "description": "Shows an annotation below a node (or centered if index omitted). Pass empty text to clear.",
      "code": "const { nodeW, nodeH, addrH, gap, baseY, colors } = registry.config; const prev = svg.selectAll('.ll-annotation'); prev.transition().duration(200).ease(d3.easeCubicIn).style('opacity', 0).remove(); if (!args.text) return; const nodesData = JSON.parse(svg.attr('data-ll-nodes') || '[]'); const n = nodesData.length; const totalW = n*nodeW+(n-1)*gap+60; const svgW = svg.node().getBoundingClientRect().width || 700; const startX = parseFloat(svg.attr('data-ll-startx')) || Math.max(16,(svgW-totalW)/2); let x = svgW/2; if (args.index !== undefined && args.index !== null) { x = startX + args.index*(nodeW+gap) + nodeW/2; } const y = baseY + nodeH + addrH + 28; const g = svg.append('g').attr('class','ll-annotation').style('opacity',0).attr('transform','translate(0, 6)'); g.append('text').attr('x', x).attr('y', y).attr('text-anchor','middle').attr('fill','#c7d2fe').style('font-family','ui-monospace,monospace').style('font-size','13px').style('font-weight','500').text(args.text); g.transition().duration(380).ease(d3.easeCubicOut).style('opacity',1).attr('transform','translate(0, 0)');"
    },
    "traverseTo": {
      "params": [
        "targetIndex"
      ],
      "description": "Animates a 'current' pointer hopping from head to targetIndex, one node at a time.",
      "code": "const { nodeW, nodeH, gap, baseY, colors } = registry.config; const svgW = svg.node().getBoundingClientRect().width || 700; const nodesData = JSON.parse(svg.attr('data-ll-nodes') || '[]'); const n = nodesData.length; const totalW = n*nodeW+(n-1)*gap+60; const startX = parseFloat(svg.attr('data-ll-startx')) || Math.max(16,(svgW-totalW)/2); const target = Math.min(args.targetIndex, n-1); svg.selectAll('.ll-traverse-ptr').remove(); const initCx = startX + nodeW*0.275; const ptr = svg.append('g').attr('class','ll-pointer ll-traverse-ptr').attr('transform',`translate(${initCx}, ${baseY - 18})`).style('opacity',0); ptr.append('path').attr('d','M -7 -14 L 0 -4 L 7 -14 Z').attr('fill','#10b981'); ptr.append('text').attr('y',-18).attr('text-anchor','middle').attr('fill','#10b981').style('font-size','11px').style('font-family','Inter,sans-serif').text('cur'); ptr.transition().duration(300).ease(d3.easeCubicOut).style('opacity',1).attr('transform',`translate(${initCx}, ${baseY-4})`).on('end', () => { const step = (i) => { ptr.attr('data-ptr-index', i); const cx = startX + i*(nodeW+gap) + nodeW*0.275; const valBox = svg.select(`#ll-node-${i}`).select('.ll-val-box'); valBox.transition().duration(200).ease(d3.easeCubicOut).attr('stroke', colors.success).attr('stroke-width', 2.5).transition().duration(320).ease(d3.easeCubicInOut).attr('stroke', colors.stroke).attr('stroke-width', 2).on('end', () => { if (i < target) { ptr.transition().duration(460).ease(d3.easeCubicInOut).attr('transform',`translate(${startX + (i+1)*(nodeW+gap) + nodeW*0.275},${baseY-4})`).on('end', () => step(i+1)); } }); }; step(0); });"
    },
    "renderDoublyLinkedListBase": {
      "params": ["nodes"],
      "description": "Renders a doubly linked list with prev|value|next node sections and bidirectional arrows. nodes=[{value, addr}]. MUST be called first for DLL. Amber arrows go backward (prev), indigo arrows go forward (next). NULL caps on both ends.",
      "code": "svg.selectAll('.dll-node,.dll-link,.dll-null,.dll-annotation,defs').remove(); const {nodeW:cfgNW,nodeH,addrH,gap,baseY,colors}=registry.config; const nodeW=cfgNW*1.3; const svgW=svg.node().getBoundingClientRect().width||700; const n=args.nodes.length; const totalW=n*nodeW+(n-1)*gap; const startX=Math.max(16,(svgW-totalW)/2); svg.attr('data-dll-startx',startX).attr('data-dll-nodew',nodeW).attr('data-dll-n',n); const defs=svg.append('defs'); defs.append('marker').attr('id','dll-arrow-next').attr('markerWidth',10).attr('markerHeight',7).attr('refX',9).attr('refY',3.5).attr('orient','auto').append('polygon').attr('points','0 0, 10 3.5, 0 7').attr('fill',colors.linkArrow); defs.append('marker').attr('id','dll-arrow-prev').attr('markerWidth',10).attr('markerHeight',7).attr('refX',9).attr('refY',3.5).attr('orient','auto').append('polygon').attr('points','0 0, 10 3.5, 0 7').attr('fill','#f59e0b'); const flt=defs.append('filter').attr('id','dll-glow'); flt.append('feGaussianBlur').attr('stdDeviation','3').attr('result','coloredBlur'); const mg=flt.append('feMerge'); mg.append('feMergeNode').attr('in','coloredBlur'); mg.append('feMergeNode').attr('in','SourceGraphic'); const nodesData=args.nodes.map((nd,i)=>({...nd,idx:i})); svg.attr('data-dll-nodes',JSON.stringify(nodesData)); const grps=svg.selectAll('.dll-node').data(nodesData,d=>d.idx).enter().append('g').attr('class','dll-node').attr('id',d=>`dll-node-${d.idx}`).attr('transform',(d,i)=>`translate(${startX+i*(nodeW+gap)},${baseY-16})`).style('opacity',0); grps.append('rect').attr('class','dll-prev-box').attr('width',nodeW*0.3).attr('height',nodeH).attr('fill','rgba(245,158,11,0.08)').attr('stroke','#f59e0b').attr('stroke-width',1.5).attr('rx',3); grps.append('text').attr('class','dll-prev-lbl').text('prev').attr('x',nodeW*0.15).attr('y',10).attr('text-anchor','middle').attr('fill','#f59e0b').style('font-size','7px').style('font-family','ui-monospace,monospace').style('opacity',0.7); grps.append('text').attr('class','dll-prev-addr').text((d,i)=>i>0?nodesData[i-1].addr:'NULL').attr('x',nodeW*0.15).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill','#f59e0b').style('font-size','8px').style('font-family','ui-monospace,monospace'); grps.append('rect').attr('class','dll-val-box').attr('x',nodeW*0.3).attr('width',nodeW*0.4).attr('height',nodeH).attr('fill','transparent').attr('stroke',colors.stroke).attr('stroke-width',2); grps.append('text').attr('class','dll-val-text').text(d=>d.value).attr('x',nodeW*0.5).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill',colors.text).style('font-size','16px').style('font-weight','600').style('font-family','Inter,sans-serif'); grps.append('rect').attr('class','dll-next-box').attr('x',nodeW*0.7).attr('width',nodeW*0.3).attr('height',nodeH).attr('fill','rgba(79,70,229,0.08)').attr('stroke',colors.stroke).attr('stroke-width',1.5); grps.append('text').attr('class','dll-next-lbl').text('next').attr('x',nodeW*0.85).attr('y',10).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','7px').style('font-family','ui-monospace,monospace').style('opacity',0.7); grps.append('text').attr('class','dll-next-addr').text((d,i)=>i<nodesData.length-1?nodesData[i+1].addr:'NULL').attr('x',nodeW*0.85).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','8px').style('font-family','ui-monospace,monospace'); grps.append('rect').attr('class','dll-addr-box').attr('y',nodeH+4).attr('width',nodeW).attr('height',addrH).attr('fill',colors.addrBg).attr('rx',3); grps.append('text').attr('class','dll-addr-text').text(d=>d.addr).attr('x',nodeW/2).attr('y',nodeH+4+addrH/2+4).attr('text-anchor','middle').attr('fill',colors.addrText).style('font-size','10px').style('font-family','ui-monospace,monospace'); grps.transition().delay((d,i)=>i*80).duration(520).ease(d3.easeBackOut.overshoot(1.1)).attr('transform',(d,i)=>`translate(${startX+i*(nodeW+gap)},${baseY})`).style('opacity',1); for(let i=0;i<nodesData.length-1;i++){const x1=startX+i*(nodeW+gap)+nodeW;const x2=startX+(i+1)*(nodeW+gap);const yF=baseY+nodeH*0.35;const len=Math.max(1,x2-x1-2);svg.append('line').attr('class','dll-link dll-link-next').attr('id',`dll-link-next-${i}`).attr('x1',x1).attr('y1',yF).attr('x2',x2-2).attr('y2',yF).attr('stroke',colors.link).attr('stroke-width',1.5).attr('marker-end','url(#dll-arrow-next)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(i*80+420).duration(480).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);} for(let i=1;i<nodesData.length;i++){const x1=startX+i*(nodeW+gap);const x2=startX+(i-1)*(nodeW+gap)+nodeW;const yB=baseY+nodeH*0.65;const len=Math.max(1,x1-x2-2);svg.append('line').attr('class','dll-link dll-link-prev').attr('id',`dll-link-prev-${i}`).attr('x1',x1).attr('y1',yB).attr('x2',x2+2).attr('y2',yB).attr('stroke','#f59e0b').attr('stroke-width',1.5).attr('marker-end','url(#dll-arrow-prev)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(i*80+500).duration(480).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);} if(n>0){const lastX=startX+(n-1)*(nodeW+gap)+nodeW;svg.append('g').attr('class','dll-null').attr('transform',`translate(${lastX+8},${baseY+nodeH*0.35-10})`).style('opacity',0).call(ng=>{ng.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5);ng.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace');}).transition().delay(n*80+380).duration(420).ease(d3.easeCubicInOut).style('opacity',1);svg.append('g').attr('class','dll-null').attr('transform',`translate(${startX-44},${baseY+nodeH*0.65-10})`).style('opacity',0).call(ng=>{ng.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5);ng.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace');}).transition().delay(380).duration(420).ease(d3.easeCubicInOut).style('opacity',1);}"
    },
    "highlightDLLNode": {
      "params": ["index", "active", "color"],
      "description": "Highlights a DLL node's value section. index=integer (0-based), active=boolean, color=optional hex string (default yellow).",
      "code": "const {colors}=registry.config; const node=svg.select(`#dll-node-${args.index}`); if(node.empty()) return; const col=args.color||colors.highlight; node.select('.dll-val-box').transition().duration(420).ease(d3.easeCubicInOut).attr('stroke',args.active?col:colors.stroke).attr('stroke-width',args.active?3:2).style('filter',args.active?'url(#dll-glow)':'none'); node.select('.dll-val-text').transition().duration(420).ease(d3.easeCubicInOut).attr('fill',args.active?col:colors.text);"
    },
    "highlightDLLLink": {
      "params": ["index", "direction", "active"],
      "description": "Highlights a DLL link. index=integer, direction='next'|'prev', active=boolean. For 'next': highlights forward arrow from node[index] to node[index+1]. For 'prev': highlights backward arrow at node[index] pointing to node[index-1].",
      "code": "const {colors}=registry.config; const dir=args.direction||'next'; const id=dir==='prev'?`dll-link-prev-${args.index}`:`dll-link-next-${args.index}`; const link=svg.select(`#${id}`); if(link.empty()) return; const defColor=dir==='prev'?'#f59e0b':colors.link; link.transition().duration(420).ease(d3.easeCubicInOut).attr('stroke',args.active?colors.highlight:defColor).attr('stroke-width',args.active?2.5:1.5);"
    },
    "moveDLLPointer": {
      "params": ["index", "label"],
      "description": "Places a named pointer arrow above the DLL node at index. index=integer, label=string (e.g. 'head', 'tail', 'cur'). Multiple labels coexist.",
      "code": "const {nodeH,gap,baseY,colors}=registry.config; const nodeW=parseFloat(svg.attr('data-dll-nodew'))||117; const svgW=svg.node().getBoundingClientRect().width||700; const nodesData=JSON.parse(svg.attr('data-dll-nodes')||'[]'); const startX=parseFloat(svg.attr('data-dll-startx'))||Math.max(16,(svgW-nodesData.length*nodeW-(nodesData.length-1)*gap)/2); const cx=startX+args.index*(nodeW+gap)+nodeW*0.5; const label=args.label||'ptr'; let ptr=svg.select(`.dll-ptr-${label}`); if(ptr.empty()){ptr=svg.append('g').attr('class',`dll-pointer dll-ptr-${label}`).style('opacity',0).attr('transform',`translate(${cx},${baseY-18})`);ptr.append('path').attr('d','M -7 -14 L 0 -4 L 7 -14 Z').attr('fill',colors.pointer);ptr.append('text').attr('y',-18).attr('text-anchor','middle').attr('fill',colors.pointer).style('font-weight','bold').style('font-size','12px').style('font-family','Inter,sans-serif').text(label);ptr.transition().duration(380).ease(d3.easeCubicOut).style('opacity',1).attr('transform',`translate(${cx},${baseY-4})`);} else {ptr.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${cx},${baseY-4})`);} ptr.attr('data-ptr-index',args.index);"
    },
    "annotateDLL": {
      "params": ["index", "text"],
      "description": "Shows a monospace annotation below a DLL node. index=integer (omit for centered). text=string. Pass empty string to clear.",
      "code": "const {nodeH,addrH,gap,baseY}=registry.config; const nodeW=parseFloat(svg.attr('data-dll-nodew'))||117; const svgW=svg.node().getBoundingClientRect().width||700; const nodesData=JSON.parse(svg.attr('data-dll-nodes')||'[]'); const startX=parseFloat(svg.attr('data-dll-startx'))||Math.max(16,(svgW-nodesData.length*nodeW-(nodesData.length-1)*gap)/2); svg.selectAll('.dll-annotation').transition().duration(200).style('opacity',0).remove(); if(!args.text) return; let x=svgW/2; if(args.index!==undefined&&args.index!==null){x=startX+args.index*(nodeW+gap)+nodeW/2;} const y=baseY+nodeH+addrH+28; const ag=svg.append('g').attr('class','dll-annotation').style('opacity',0).attr('transform','translate(0,6)'); ag.append('text').attr('x',x).attr('y',y).attr('text-anchor','middle').attr('fill','#c7d2fe').style('font-family','ui-monospace,monospace').style('font-size','13px').style('font-weight','500').text(args.text); ag.transition().duration(380).ease(d3.easeCubicOut).style('opacity',1).attr('transform','translate(0,0)');"
    },
    "clearDLLHighlights": {
      "params": [],
      "description": "Resets all DLL node and link highlights to their default colors.",
      "code": "const {colors}=registry.config; svg.selectAll('.dll-node').each(function(){const nd=d3.select(this);nd.select('.dll-val-box').transition().duration(380).attr('stroke',colors.stroke).attr('stroke-width',2).style('filter','none');nd.select('.dll-val-text').transition().duration(380).attr('fill',colors.text);}); svg.selectAll('.dll-link-next').transition().duration(380).attr('stroke',colors.link).attr('stroke-width',1.5); svg.selectAll('.dll-link-prev').transition().duration(380).attr('stroke','#f59e0b').attr('stroke-width',1.5);"
    },
    "renderCircularLinkedListBase": {
      "params": ["nodes"],
      "description": "Renders a circular linked list. Nodes look like a singly linked list but the last node's next pointer wraps back to head via an animated arc below. nodes=[{value, addr}]. MUST be called first for CLL.",
      "code": "svg.selectAll('.cll-node,.cll-link,.cll-wrap,.cll-wrap-label,.cll-annotation,defs').remove(); const {nodeW,nodeH,addrH,gap,baseY,colors}=registry.config; const svgW=svg.node().getBoundingClientRect().width||700; const n=args.nodes.length; const totalW=n*nodeW+(n-1)*gap; const startX=Math.max(16,(svgW-totalW)/2); svg.attr('data-cll-startx',startX).attr('data-cll-n',n); const defs=svg.append('defs'); defs.append('marker').attr('id','cll-arrow').attr('markerWidth',10).attr('markerHeight',7).attr('refX',9).attr('refY',3.5).attr('orient','auto').append('polygon').attr('points','0 0, 10 3.5, 0 7').attr('fill',colors.linkArrow); defs.append('marker').attr('id','cll-arrow-wrap').attr('markerWidth',10).attr('markerHeight',7).attr('refX',9).attr('refY',3.5).attr('orient','auto').append('polygon').attr('points','0 0, 10 3.5, 0 7').attr('fill','#10b981'); const flt=defs.append('filter').attr('id','cll-glow'); flt.append('feGaussianBlur').attr('stdDeviation','3').attr('result','coloredBlur'); const mg=flt.append('feMerge'); mg.append('feMergeNode').attr('in','coloredBlur'); mg.append('feMergeNode').attr('in','SourceGraphic'); const nodesData=args.nodes.map((nd,i)=>({...nd,idx:i})); svg.attr('data-cll-nodes',JSON.stringify(nodesData)); const grps=svg.selectAll('.cll-node').data(nodesData,d=>d.idx).enter().append('g').attr('class','cll-node').attr('id',d=>`cll-node-${d.idx}`).attr('transform',(d,i)=>`translate(${startX+i*(nodeW+gap)},${baseY-16})`).style('opacity',0); grps.append('rect').attr('class','cll-val-box').attr('width',nodeW*0.55).attr('height',nodeH).attr('fill','transparent').attr('stroke',colors.stroke).attr('stroke-width',2).attr('rx',4); grps.append('rect').attr('class','cll-next-box').attr('x',nodeW*0.55).attr('width',nodeW*0.45).attr('height',nodeH).attr('fill','rgba(79,70,229,0.08)').attr('stroke',colors.stroke).attr('stroke-width',2).attr('rx',0); grps.append('text').attr('class','cll-val-text').text(d=>d.value).attr('x',nodeW*0.275).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill',colors.text).style('font-size','16px').style('font-weight','600').style('font-family','Inter,sans-serif'); grps.append('text').attr('class','cll-next-label').text((d,i)=>i<nodesData.length-1?nodesData[i+1].addr:nodesData[0].addr).attr('x',nodeW*0.775).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','9px').style('font-family','ui-monospace,monospace'); grps.append('rect').attr('class','cll-addr-box').attr('y',nodeH+4).attr('width',nodeW).attr('height',addrH).attr('fill',colors.addrBg).attr('rx',3); grps.append('text').attr('class','cll-addr-text').text(d=>d.addr).attr('x',nodeW/2).attr('y',nodeH+4+addrH/2+4).attr('text-anchor','middle').attr('fill',colors.addrText).style('font-size','10px').style('font-family','ui-monospace,monospace'); grps.transition().delay((d,i)=>i*80).duration(520).ease(d3.easeBackOut.overshoot(1.1)).attr('transform',(d,i)=>`translate(${startX+i*(nodeW+gap)},${baseY})`).style('opacity',1); for(let i=0;i<nodesData.length-1;i++){const x1=startX+i*(nodeW+gap)+nodeW;const x2=startX+(i+1)*(nodeW+gap);const y=baseY+nodeH/2;const len=Math.max(1,x2-x1-2);svg.append('line').attr('class','cll-link').attr('id',`cll-link-${i}`).attr('x1',x1).attr('y1',y).attr('x2',x2-2).attr('y2',y).attr('stroke',colors.link).attr('stroke-width',2).attr('marker-end','url(#cll-arrow)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(i*80+420).duration(480).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);} if(n>1){const arcSx=startX+(n-1)*(nodeW+gap)+nodeW*0.775;const arcSy=baseY+nodeH;const arcEx=startX+nodeW*0.275;const arcEy=baseY+nodeH;const cxCtrl=(arcSx+arcEx)/2;const cyCtrl=baseY+nodeH+addrH+30;const pathD=`M ${arcSx} ${arcSy} Q ${cxCtrl} ${cyCtrl} ${arcEx} ${arcEy-2}`;const wrapEl=svg.append('path').attr('class','cll-wrap').attr('id','cll-link-wrap').attr('d',pathD).attr('fill','none').attr('stroke','#10b981').attr('stroke-width',2).attr('marker-end','url(#cll-arrow-wrap)');const pathLen=wrapEl.node().getTotalLength();wrapEl.attr('stroke-dasharray',pathLen).attr('stroke-dashoffset',pathLen).transition().delay(n*80+420).duration(600).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);svg.append('text').attr('class','cll-wrap-label').text('↩ head').attr('x',cxCtrl).attr('y',cyCtrl+16).attr('text-anchor','middle').attr('fill','#10b981').style('font-size','11px').style('font-family','ui-monospace,monospace').style('opacity',0).transition().delay(n*80+700).duration(380).style('opacity',0.8);}"
    },
    "highlightCLLNode": {
      "params": ["index", "active", "color"],
      "description": "Highlights a CLL node. index=integer (0-based), active=boolean, color=optional hex string (default yellow).",
      "code": "const {colors}=registry.config; const node=svg.select(`#cll-node-${args.index}`); if(node.empty()) return; const col=args.color||colors.highlight; node.select('.cll-val-box').transition().duration(420).ease(d3.easeCubicInOut).attr('stroke',args.active?col:colors.stroke).attr('stroke-width',args.active?3:2).style('filter',args.active?'url(#cll-glow)':'none'); node.select('.cll-val-text').transition().duration(420).ease(d3.easeCubicInOut).attr('fill',args.active?col:colors.text);"
    },
    "highlightCLLLink": {
      "params": ["index", "active"],
      "description": "Highlights a CLL link. index=integer (link from node[index] to node[index+1]), use index=-1 for the wrap-around arc from tail back to head. active=boolean.",
      "code": "const {colors}=registry.config; if(args.index===-1){const wrap=svg.select('#cll-link-wrap');if(!wrap.empty()) wrap.transition().duration(420).attr('stroke',args.active?colors.highlight:'#10b981').attr('stroke-width',args.active?3:2);return;} const link=svg.select(`#cll-link-${args.index}`); if(link.empty()) return; link.transition().duration(420).ease(d3.easeCubicInOut).attr('stroke',args.active?colors.highlight:colors.link).attr('stroke-width',args.active?3:2);"
    },
    "moveCLLPointer": {
      "params": ["index", "label"],
      "description": "Places a named pointer arrow above the CLL node at index. index=integer, label=string (e.g. 'head', 'cur'). Multiple labels coexist.",
      "code": "const {nodeW,nodeH,gap,baseY,colors}=registry.config; const svgW=svg.node().getBoundingClientRect().width||700; const nodesData=JSON.parse(svg.attr('data-cll-nodes')||'[]'); const n=nodesData.length; const startX=parseFloat(svg.attr('data-cll-startx'))||Math.max(16,(svgW-n*nodeW-(n-1)*gap)/2); const cx=startX+args.index*(nodeW+gap)+nodeW*0.275; const label=args.label||'ptr'; let ptr=svg.select(`.cll-ptr-${label}`); if(ptr.empty()){ptr=svg.append('g').attr('class',`cll-pointer cll-ptr-${label}`).style('opacity',0).attr('transform',`translate(${cx},${baseY-18})`);ptr.append('path').attr('d','M -7 -14 L 0 -4 L 7 -14 Z').attr('fill',colors.pointer);ptr.append('text').attr('y',-18).attr('text-anchor','middle').attr('fill',colors.pointer).style('font-weight','bold').style('font-size','12px').style('font-family','Inter,sans-serif').text(label);ptr.transition().duration(380).ease(d3.easeCubicOut).style('opacity',1).attr('transform',`translate(${cx},${baseY-4})`);} else {ptr.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${cx},${baseY-4})`);} ptr.attr('data-ptr-index',args.index);"
    },
    "annotateCLL": {
      "params": ["index", "text"],
      "description": "Shows a monospace annotation below the CLL wrap arc. index=integer (omit for centered). text=string. Pass empty string to clear.",
      "code": "const {nodeW,nodeH,addrH,gap,baseY}=registry.config; const svgW=svg.node().getBoundingClientRect().width||700; const nodesData=JSON.parse(svg.attr('data-cll-nodes')||'[]'); const n=nodesData.length; const startX=parseFloat(svg.attr('data-cll-startx'))||Math.max(16,(svgW-n*nodeW-(n-1)*gap)/2); svg.selectAll('.cll-annotation').transition().duration(200).style('opacity',0).remove(); if(!args.text) return; let x=svgW/2; if(args.index!==undefined&&args.index!==null){x=startX+args.index*(nodeW+gap)+nodeW/2;} const y=baseY+nodeH+addrH+56; const ag=svg.append('g').attr('class','cll-annotation').style('opacity',0).attr('transform','translate(0,6)'); ag.append('text').attr('x',x).attr('y',y).attr('text-anchor','middle').attr('fill','#c7d2fe').style('font-family','ui-monospace,monospace').style('font-size','13px').style('font-weight','500').text(args.text); ag.transition().duration(380).ease(d3.easeCubicOut).style('opacity',1).attr('transform','translate(0,0)');"
    },
    "clearCLLHighlights": {
      "params": [],
      "description": "Resets all CLL node and link highlights (including the wrap-around arc) to default colors.",
      "code": "const {colors}=registry.config; svg.selectAll('.cll-node').each(function(){const nd=d3.select(this);nd.select('.cll-val-box').transition().duration(380).attr('stroke',colors.stroke).attr('stroke-width',2).style('filter','none');nd.select('.cll-val-text').transition().duration(380).attr('fill',colors.text);}); svg.selectAll('.cll-link').transition().duration(380).attr('stroke',colors.link).attr('stroke-width',2); const wrap=svg.select('#cll-link-wrap'); if(!wrap.empty()) wrap.transition().duration(380).attr('stroke','#10b981').attr('stroke-width',2);"
    },
    "addDLLNode": {
      "params": ["value","addr","atIndex"],
      "description": "Insert node into doubly linked list at atIndex with animation and link updates.",
      "code": "const {nodeW:cfgNW,nodeH,addrH,gap,baseY,colors}=registry.config; const nodeW=parseFloat(svg.attr('data-dll-nodew'))||cfgNW*1.3; let nodesData=JSON.parse(svg.attr('data-dll-nodes')||'[]'); const newNode={value:args.value,addr:args.addr}; const idx=args.atIndex===undefined?nodesData.length:Math.max(0,Math.min(args.atIndex,nodesData.length)); nodesData.splice(idx,0,newNode); nodesData=nodesData.map((nd,i)=>({...nd,idx:i})); svg.attr('data-dll-nodes',JSON.stringify(nodesData)).attr('data-dll-n',nodesData.length); svg.selectAll('.dll-node,.dll-link,.dll-null,.dll-annotation').remove(); const svgW=svg.node().getBoundingClientRect().width||700; const n=nodesData.length; const totalW=n*nodeW+(n-1)*gap; const startX=Math.max(16,(svgW-totalW)/2); svg.attr('data-dll-startx',startX); const grps=svg.selectAll('.dll-node').data(nodesData,d=>d.idx).enter().append('g').attr('class','dll-node').attr('id',d=>`dll-node-${d.idx}`).attr('transform',(d,i)=>i===idx?`translate(${startX+i*(nodeW+gap)},${baseY-20})`:`translate(${startX+i*(nodeW+gap)},${baseY})`).style('opacity',(d,i)=>i===idx?0:1); grps.append('rect').attr('class','dll-prev-box').attr('width',nodeW*0.3).attr('height',nodeH).attr('fill','rgba(245,158,11,0.08)').attr('stroke',d=>d.idx===idx?colors.success:'#f59e0b').attr('stroke-width',1.5).attr('rx',3); grps.append('text').attr('class','dll-prev-lbl').text('prev').attr('x',nodeW*0.15).attr('y',10).attr('text-anchor','middle').attr('fill','#f59e0b').style('font-size','7px').style('font-family','ui-monospace,monospace').style('opacity',0.7); grps.append('text').attr('class','dll-prev-addr').text((d,i)=>i>0?nodesData[i-1].addr:'NULL').attr('x',nodeW*0.15).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill','#f59e0b').style('font-size','8px').style('font-family','ui-monospace,monospace'); grps.append('rect').attr('class','dll-val-box').attr('x',nodeW*0.3).attr('width',nodeW*0.4).attr('height',nodeH).attr('fill','transparent').attr('stroke',d=>d.idx===idx?colors.success:colors.stroke).attr('stroke-width',2); grps.append('text').attr('class','dll-val-text').text(d=>d.value).attr('x',nodeW*0.5).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill',colors.text).style('font-size','16px').style('font-weight','600').style('font-family','Inter,sans-serif'); grps.append('rect').attr('class','dll-next-box').attr('x',nodeW*0.7).attr('width',nodeW*0.3).attr('height',nodeH).attr('fill','rgba(79,70,229,0.08)').attr('stroke',d=>d.idx===idx?colors.success:colors.stroke).attr('stroke-width',1.5); grps.append('text').attr('class','dll-next-lbl').text('next').attr('x',nodeW*0.85).attr('y',10).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','7px').style('font-family','ui-monospace,monospace').style('opacity',0.7); grps.append('text').attr('class','dll-next-addr').text((d,i)=>i<nodesData.length-1?nodesData[i+1].addr:'NULL').attr('x',nodeW*0.85).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','8px').style('font-family','ui-monospace,monospace'); grps.append('rect').attr('class','dll-addr-box').attr('y',nodeH+4).attr('width',nodeW).attr('height',addrH).attr('fill',colors.addrBg).attr('rx',3); grps.append('text').attr('class','dll-addr-text').text(d=>d.addr).attr('x',nodeW/2).attr('y',nodeH+4+addrH/2+4).attr('text-anchor','middle').attr('fill',colors.addrText).style('font-size','10px').style('font-family','ui-monospace,monospace'); svg.select(`#dll-node-${idx}`).transition().delay(80).duration(520).ease(d3.easeBackOut.overshoot(1.15)).attr('transform',`translate(${startX+idx*(nodeW+gap)},${baseY})`).style('opacity',1); for(let i=0;i<nodesData.length-1;i++){const x1=startX+i*(nodeW+gap)+nodeW;const x2=startX+(i+1)*(nodeW+gap);const yF=baseY+nodeH*0.35;const len=Math.max(1,x2-x1-2);const isAffectedNext=i===idx-1||i===idx;svg.append('line').attr('class','dll-link dll-link-next').attr('id',`dll-link-next-${i}`).attr('x1',x1).attr('y1',yF).attr('x2',x2-2).attr('y2',yF).attr('stroke',isAffectedNext?colors.success:colors.link).attr('stroke-width',1.5).attr('marker-end','url(#dll-arrow-next)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(isAffectedNext?500:0).duration(460).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0).transition().duration(400).attr('stroke',colors.link);} for(let i=1;i<nodesData.length;i++){const x1=startX+i*(nodeW+gap);const x2=startX+(i-1)*(nodeW+gap)+nodeW;const yB=baseY+nodeH*0.65;const len=Math.max(1,x1-x2-2);const isAffectedPrev=i===idx||i===idx+1;svg.append('line').attr('class','dll-link dll-link-prev').attr('id',`dll-link-prev-${i}`).attr('x1',x1).attr('y1',yB).attr('x2',x2+2).attr('y2',yB).attr('stroke',isAffectedPrev?colors.success:'#f59e0b').attr('stroke-width',1.5).attr('marker-end','url(#dll-arrow-prev)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(isAffectedPrev?500:0).duration(460).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0).transition().duration(400).attr('stroke','#f59e0b');} if(n>0){const lastX=startX+(n-1)*(nodeW+gap)+nodeW;svg.append('g').attr('class','dll-null').attr('transform',`translate(${lastX+8},${baseY+nodeH*0.35-10})`).style('opacity',0).call(ng=>{ng.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5);ng.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace');}).transition().delay(500).duration(420).ease(d3.easeCubicInOut).style('opacity',1);svg.append('g').attr('class','dll-null').attr('transform',`translate(${startX-44},${baseY+nodeH*0.65-10})`).style('opacity',0).call(ng=>{ng.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5);ng.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace');}).transition().delay(380).duration(420).ease(d3.easeCubicInOut).style('opacity',1);} svg.selectAll('.dll-pointer').each(function(){const ptrEl=d3.select(this);let ptrIdx=parseInt(ptrEl.attr('data-ptr-index'));if(isNaN(ptrIdx)) return;if(ptrIdx>=idx) ptrIdx+=1;ptrEl.attr('data-ptr-index',ptrIdx);const newCx=startX+ptrIdx*(nodeW+gap)+nodeW*0.5;ptrEl.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${newCx},${baseY-4})`);});"
    },
    "deleteDLLNode": {
      "params": ["index"],
      "description": "Delete node from doubly linked list with fade/slide animation and relink.",
      "code": "const {nodeW:cfgNW,nodeH,addrH,gap,baseY,colors}=registry.config; const nodeW=parseFloat(svg.attr('data-dll-nodew'))||cfgNW*1.3; let nodesData=JSON.parse(svg.attr('data-dll-nodes')||'[]'); if(args.index<0||args.index>=nodesData.length) return; const svgW=svg.node().getBoundingClientRect().width||700; const target=svg.select(`#dll-node-${args.index}`); const currentTransform=target.attr('transform')||''; const txMatch=currentTransform.match(/translate\\(([^,]+)/); const tx=txMatch?parseFloat(txMatch[1]):0; svg.select(`#dll-link-next-${args.index-1}`).transition().duration(320).style('opacity',0); svg.select(`#dll-link-next-${args.index}`).transition().duration(320).style('opacity',0); svg.select(`#dll-link-prev-${args.index}`).transition().duration(320).style('opacity',0); svg.select(`#dll-link-prev-${args.index+1}`).transition().duration(320).style('opacity',0); target.select('.dll-val-box').transition().duration(260).ease(d3.easeCubicOut).attr('stroke','#ef4444').attr('stroke-width',3).transition().duration(60).attr('stroke-width',2); target.transition().delay(200).duration(460).ease(d3.easeCubicIn).attr('transform',`translate(${tx},${baseY-26})`).style('opacity',0).on('end',function(){d3.select(this).remove();svg.selectAll('.dll-link,.dll-null').remove();nodesData.splice(args.index,1);nodesData=nodesData.map((nd,i)=>({...nd,idx:i}));svg.attr('data-dll-nodes',JSON.stringify(nodesData)).attr('data-dll-n',nodesData.length);const n=nodesData.length;const newTotalW=n*nodeW+Math.max(0,n-1)*gap;const newStartX=Math.max(16,(svgW-newTotalW)/2);svg.attr('data-dll-startx',newStartX);svg.selectAll('.dll-node').each(function(){const el=d3.select(this);const oldId=parseInt((el.attr('id')||'').replace('dll-node-',''));const newIdx=oldId>args.index?oldId-1:oldId;el.attr('id',`dll-node-${newIdx}`);el.select('.dll-prev-addr').text(newIdx>0?nodesData[newIdx-1].addr:'NULL');el.select('.dll-next-addr').text(newIdx<nodesData.length-1?nodesData[newIdx+1].addr:'NULL');el.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${newStartX+newIdx*(nodeW+gap)},${baseY})`);});svg.selectAll('.dll-pointer').each(function(){const ptrEl=d3.select(this);let ptrIdx=parseInt(ptrEl.attr('data-ptr-index'));if(isNaN(ptrIdx)) return;if(ptrIdx===args.index){ptrIdx=Math.min(ptrIdx,nodesData.length-1);}else if(ptrIdx>args.index){ptrIdx-=1;}if(ptrIdx<0||nodesData.length===0){ptrEl.transition().duration(380).style('opacity',0).remove();return;}ptrEl.attr('data-ptr-index',ptrIdx);const newCx=newStartX+ptrIdx*(nodeW+gap)+nodeW*0.5;ptrEl.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${newCx},${baseY-4})`);});const drawDLLLinks=()=>{const nn=nodesData.length;for(let i=0;i<nn-1;i++){const x1=newStartX+i*(nodeW+gap)+nodeW;const x2=newStartX+(i+1)*(nodeW+gap);const yF=baseY+nodeH*0.35;const len=Math.max(1,x2-x1-2);svg.append('line').attr('class','dll-link dll-link-next').attr('id',`dll-link-next-${i}`).attr('x1',x1).attr('y1',yF).attr('x2',x2-2).attr('y2',yF).attr('stroke',colors.link).attr('stroke-width',1.5).attr('marker-end','url(#dll-arrow-next)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(i*55).duration(440).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);}for(let i=1;i<nn;i++){const x1=newStartX+i*(nodeW+gap);const x2=newStartX+(i-1)*(nodeW+gap)+nodeW;const yB=baseY+nodeH*0.65;const len=Math.max(1,x1-x2-2);svg.append('line').attr('class','dll-link dll-link-prev').attr('id',`dll-link-prev-${i}`).attr('x1',x1).attr('y1',yB).attr('x2',x2+2).attr('y2',yB).attr('stroke','#f59e0b').attr('stroke-width',1.5).attr('marker-end','url(#dll-arrow-prev)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(i*55).duration(440).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);}if(nn>0){const lastX=newStartX+(nn-1)*(nodeW+gap)+nodeW;svg.append('g').attr('class','dll-null').attr('transform',`translate(${lastX+8},${baseY+nodeH*0.35-10})`).style('opacity',0).call(ng=>{ng.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5);ng.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace');}).transition().delay(nn*55).duration(380).ease(d3.easeCubicInOut).style('opacity',1);svg.append('g').attr('class','dll-null').attr('transform',`translate(${newStartX-44},${baseY+nodeH*0.65-10})`).style('opacity',0).call(ng=>{ng.append('rect').attr('width',36).attr('height',20).attr('rx',3).attr('fill','rgba(71,85,105,0.3)').attr('stroke',colors.null).attr('stroke-width',1.5);ng.append('text').text('NULL').attr('x',18).attr('y',14).attr('text-anchor','middle').attr('fill',colors.null).style('font-size','9px').style('font-family','ui-monospace,monospace');}).transition().delay(380).duration(380).ease(d3.easeCubicInOut).style('opacity',1);}};setTimeout(drawDLLLinks,260);});"
    },
    "addCLLNode": {
      "params": ["value","addr","atIndex"],
      "description": "Insert node into circular linked list at atIndex and redraw wrap-around arc.",
      "code": "const {nodeW,nodeH,addrH,gap,baseY,colors}=registry.config; let nodesData=JSON.parse(svg.attr('data-cll-nodes')||'[]'); const newNode={value:args.value,addr:args.addr}; const idx=args.atIndex===undefined?nodesData.length:Math.max(0,Math.min(args.atIndex,nodesData.length)); nodesData.splice(idx,0,newNode); nodesData=nodesData.map((nd,i)=>({...nd,idx:i})); svg.attr('data-cll-nodes',JSON.stringify(nodesData)).attr('data-cll-n',nodesData.length); svg.selectAll('.cll-node,.cll-link,.cll-wrap,.cll-wrap-label,.cll-annotation').remove(); const svgW=svg.node().getBoundingClientRect().width||700; const n=nodesData.length; const totalW=n*nodeW+(n-1)*gap; const startX=Math.max(16,(svgW-totalW)/2); svg.attr('data-cll-startx',startX); const grps=svg.selectAll('.cll-node').data(nodesData,d=>d.idx).enter().append('g').attr('class','cll-node').attr('id',d=>`cll-node-${d.idx}`).attr('transform',(d,i)=>i===idx?`translate(${startX+i*(nodeW+gap)},${baseY-20})`:`translate(${startX+i*(nodeW+gap)},${baseY})`).style('opacity',(d,i)=>i===idx?0:1); grps.append('rect').attr('class','cll-val-box').attr('width',nodeW*0.55).attr('height',nodeH).attr('fill','transparent').attr('stroke',d=>d.idx===idx?colors.success:colors.stroke).attr('stroke-width',2).attr('rx',4); grps.append('rect').attr('class','cll-next-box').attr('x',nodeW*0.55).attr('width',nodeW*0.45).attr('height',nodeH).attr('fill','rgba(79,70,229,0.08)').attr('stroke',d=>d.idx===idx?colors.success:colors.stroke).attr('stroke-width',2).attr('rx',0); grps.append('text').attr('class','cll-val-text').text(d=>d.value).attr('x',nodeW*0.275).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill',colors.text).style('font-size','16px').style('font-weight','600').style('font-family','Inter,sans-serif'); grps.append('text').attr('class','cll-next-label').text((d,i)=>nodesData[(i+1)%n].addr).attr('x',nodeW*0.775).attr('y',nodeH/2+5).attr('text-anchor','middle').attr('fill','#6366f1').style('font-size','9px').style('font-family','ui-monospace,monospace'); grps.append('rect').attr('class','cll-addr-box').attr('y',nodeH+4).attr('width',nodeW).attr('height',addrH).attr('fill',colors.addrBg).attr('rx',3); grps.append('text').attr('class','cll-addr-text').text(d=>d.addr).attr('x',nodeW/2).attr('y',nodeH+4+addrH/2+4).attr('text-anchor','middle').attr('fill',colors.addrText).style('font-size','10px').style('font-family','ui-monospace,monospace'); svg.select(`#cll-node-${idx}`).transition().delay(80).duration(520).ease(d3.easeBackOut.overshoot(1.15)).attr('transform',`translate(${startX+idx*(nodeW+gap)},${baseY})`).style('opacity',1); for(let i=0;i<nodesData.length-1;i++){const x1=startX+i*(nodeW+gap)+nodeW;const x2=startX+(i+1)*(nodeW+gap);const y=baseY+nodeH/2;const len=Math.max(1,x2-x1-2);const isAffected=i===idx-1||i===idx;svg.append('line').attr('class','cll-link').attr('id',`cll-link-${i}`).attr('x1',x1).attr('y1',y).attr('x2',x2-2).attr('y2',y).attr('stroke',isAffected?colors.success:colors.link).attr('stroke-width',2).attr('marker-end','url(#cll-arrow)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(isAffected?500:0).duration(460).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0).transition().duration(400).attr('stroke',colors.link);} if(n>1){const arcSx=startX+(n-1)*(nodeW+gap)+nodeW*0.775;const arcSy=baseY+nodeH;const arcEx=startX+nodeW*0.275;const arcEy=baseY+nodeH;const cxCtrl=(arcSx+arcEx)/2;const cyCtrl=baseY+nodeH+addrH+30;const pathD=`M ${arcSx} ${arcSy} Q ${cxCtrl} ${cyCtrl} ${arcEx} ${arcEy-2}`;const wrapEl=svg.append('path').attr('class','cll-wrap').attr('id','cll-link-wrap').attr('d',pathD).attr('fill','none').attr('stroke','#10b981').attr('stroke-width',2).attr('marker-end','url(#cll-arrow-wrap)');const pathLen=wrapEl.node().getTotalLength();wrapEl.attr('stroke-dasharray',pathLen).attr('stroke-dashoffset',pathLen).transition().delay(n*80+420).duration(600).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);svg.append('text').attr('class','cll-wrap-label').text('↩ head').attr('x',cxCtrl).attr('y',cyCtrl+16).attr('text-anchor','middle').attr('fill','#10b981').style('font-size','11px').style('font-family','ui-monospace,monospace').style('opacity',0).transition().delay(n*80+700).duration(380).style('opacity',0.8);} svg.selectAll('.cll-pointer').each(function(){const ptrEl=d3.select(this);let ptrIdx=parseInt(ptrEl.attr('data-ptr-index'));if(isNaN(ptrIdx)) return;if(ptrIdx>=idx) ptrIdx+=1;ptrEl.attr('data-ptr-index',ptrIdx);const newCx=startX+ptrIdx*(nodeW+gap)+nodeW*0.275;ptrEl.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${newCx},${baseY-4})`);});"
    },
    "deleteCLLNode": {
      "params": ["index"],
      "description": "Delete node from circular linked list with animation and rebuild wrap-around.",
      "code": "const {nodeW,nodeH,addrH,gap,baseY,colors}=registry.config; let nodesData=JSON.parse(svg.attr('data-cll-nodes')||'[]'); if(args.index<0||args.index>=nodesData.length) return; const svgW=svg.node().getBoundingClientRect().width||700; const target=svg.select(`#cll-node-${args.index}`); const currentTransform=target.attr('transform')||''; const txMatch=currentTransform.match(/translate\\(([^,]+)/); const tx=txMatch?parseFloat(txMatch[1]):0; const n=nodesData.length; svg.select(`#cll-link-${args.index-1}`).transition().duration(320).style('opacity',0); svg.select(`#cll-link-${args.index}`).transition().duration(320).style('opacity',0); if(args.index===0||args.index===n-1){svg.select('#cll-link-wrap').transition().duration(320).style('opacity',0);svg.select('.cll-wrap-label').transition().duration(320).style('opacity',0);} target.select('.cll-val-box').transition().duration(260).ease(d3.easeCubicOut).attr('stroke','#ef4444').attr('stroke-width',3).transition().duration(60).attr('stroke-width',2); target.transition().delay(200).duration(460).ease(d3.easeCubicIn).attr('transform',`translate(${tx},${baseY-26})`).style('opacity',0).on('end',function(){d3.select(this).remove();svg.selectAll('.cll-link,.cll-wrap,.cll-wrap-label').remove();nodesData.splice(args.index,1);nodesData=nodesData.map((nd,i)=>({...nd,idx:i}));svg.attr('data-cll-nodes',JSON.stringify(nodesData)).attr('data-cll-n',nodesData.length);const nn=nodesData.length;const newTotalW=nn*nodeW+Math.max(0,nn-1)*gap;const newStartX=Math.max(16,(svgW-newTotalW)/2);svg.attr('data-cll-startx',newStartX);svg.selectAll('.cll-node').each(function(){const el=d3.select(this);const oldId=parseInt((el.attr('id')||'').replace('cll-node-',''));const newIdx=oldId>args.index?oldId-1:oldId;el.attr('id',`cll-node-${newIdx}`);if(nn>0){el.select('.cll-next-label').text(nodesData[(newIdx+1)%nn].addr);}el.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${newStartX+newIdx*(nodeW+gap)},${baseY})`);});svg.selectAll('.cll-pointer').each(function(){const ptrEl=d3.select(this);let ptrIdx=parseInt(ptrEl.attr('data-ptr-index'));if(isNaN(ptrIdx)) return;if(ptrIdx===args.index){ptrIdx=Math.min(ptrIdx,nodesData.length-1);}else if(ptrIdx>args.index){ptrIdx-=1;}if(ptrIdx<0||nodesData.length===0){ptrEl.transition().duration(380).style('opacity',0).remove();return;}ptrEl.attr('data-ptr-index',ptrIdx);const newCx=newStartX+ptrIdx*(nodeW+gap)+nodeW*0.275;ptrEl.transition().duration(520).ease(d3.easeCubicInOut).attr('transform',`translate(${newCx},${baseY-4})`);});const drawCLLLinks=()=>{for(let i=0;i<nodesData.length-1;i++){const x1=newStartX+i*(nodeW+gap)+nodeW;const x2=newStartX+(i+1)*(nodeW+gap);const y=baseY+nodeH/2;const len=Math.max(1,x2-x1-2);svg.append('line').attr('class','cll-link').attr('id',`cll-link-${i}`).attr('x1',x1).attr('y1',y).attr('x2',x2-2).attr('y2',y).attr('stroke',colors.link).attr('stroke-width',2).attr('marker-end','url(#cll-arrow)').attr('stroke-dasharray',len).attr('stroke-dashoffset',len).transition().delay(i*55).duration(440).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);}if(nn>1){const arcSx=newStartX+(nn-1)*(nodeW+gap)+nodeW*0.775;const arcSy=baseY+nodeH;const arcEx=newStartX+nodeW*0.275;const arcEy=baseY+nodeH;const cxCtrl=(arcSx+arcEx)/2;const cyCtrl=baseY+nodeH+addrH+30;const pathD=`M ${arcSx} ${arcSy} Q ${cxCtrl} ${cyCtrl} ${arcEx} ${arcEy-2}`;const wrapEl=svg.append('path').attr('class','cll-wrap').attr('id','cll-link-wrap').attr('d',pathD).attr('fill','none').attr('stroke','#10b981').attr('stroke-width',2).attr('marker-end','url(#cll-arrow-wrap)');const pathLen=wrapEl.node().getTotalLength();wrapEl.attr('stroke-dasharray',pathLen).attr('stroke-dashoffset',pathLen).transition().delay(nn*55).duration(500).ease(d3.easeCubicInOut).attr('stroke-dashoffset',0);svg.append('text').attr('class','cll-wrap-label').text('↩ head').attr('x',cxCtrl).attr('y',cyCtrl+16).attr('text-anchor','middle').attr('fill','#10b981').style('font-size','11px').style('font-family','ui-monospace,monospace').style('opacity',0).transition().delay(nn*55+300).duration(380).style('opacity',0.8);}};setTimeout(drawCLLLinks,260);});"
    }
  }
}