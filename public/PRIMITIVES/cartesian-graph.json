{
  "primitive": "CARTESIAN_GRAPH_V1",
  "config": {
    "width": 760,
    "height": 420,
    "margin": {
      "top": 44,
      "right": 34,
      "bottom": 56,
      "left": 64
    },
    "viewport": {
      "xMin": -10,
      "xMax": 10,
      "yMin": -10,
      "yMax": 10
    },
    "colors": {
      "axis": "#4f46e5",
      "grid": "rgba(255, 255, 255, 0.08)",
      "tick": "rgba(226, 232, 240, 0.7)",
      "label": "rgba(226, 232, 240, 0.85)",
      "point": "#3b82f6",
      "pointStroke": "rgba(255, 255, 255, 0.12)",
      "line": "#10b981",
      "curve": "#eab308",
      "highlight": "#eab308",
      "annotation": "#c7d2fe"
    }
  },
  "functions": {
    "renderCartesianBase": {
      "params": [
        "xMin",
        "xMax",
        "yMin",
        "yMax",
        "grid",
        "title",
        "xLabel",
        "yLabel",
        "points"
      ],
      "code": "const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const m = cfg.margin || {}; const width = Number(cfg.width) || 760; const height = Number(cfg.height) || 420; const margin = { top: Number(m.top) || 44, right: Number(m.right) || 34, bottom: Number(m.bottom) || 56, left: Number(m.left) || 64 }; const vp = cfg.viewport || {}; let xMin = Number(args && args.xMin); let xMax = Number(args && args.xMax); let yMin = Number(args && args.yMin); let yMax = Number(args && args.yMax); if (!Number.isFinite(xMin)) xMin = Number(vp.xMin); if (!Number.isFinite(xMax)) xMax = Number(vp.xMax); if (!Number.isFinite(yMin)) yMin = Number(vp.yMin); if (!Number.isFinite(yMax)) yMax = Number(vp.yMax); if (!Number.isFinite(xMin)) xMin = -10; if (!Number.isFinite(xMax)) xMax = 10; if (!Number.isFinite(yMin)) yMin = -10; if (!Number.isFinite(yMax)) yMax = 10; if (xMax <= xMin) xMax = xMin + 1; if (yMax <= yMin) yMax = yMin + 1; svg.selectAll('.cg-root, .cg-title, .cg-xlabel, .cg-ylabel, .cg-annotation').remove(); svg.attr('data-cg-xmin', xMin).attr('data-cg-xmax', xMax).attr('data-cg-ymin', yMin).attr('data-cg-ymax', yMax).attr('data-cg-grid', args && args.grid === false ? '0' : '1'); const root = svg.append('g').attr('class', 'cg-root'); const innerW = Math.max(40, width - margin.left - margin.right); const innerH = Math.max(40, height - margin.top - margin.bottom); svg.style('overflow', 'visible'); const defs = root.append('defs'); const filter = defs.append('filter').attr('id', 'cg-glow'); filter.append('feGaussianBlur').attr('stdDeviation', '4').attr('result', 'coloredBlur'); const merge = filter.append('feMerge'); merge.append('feMergeNode').attr('in', 'coloredBlur'); merge.append('feMergeNode').attr('in', 'SourceGraphic'); const chart = root.append('g').attr('class', 'cg-chart').attr('transform', `translate(${margin.left},${margin.top})`); chart.append('rect').attr('class', 'cg-bg').attr('width', innerW).attr('height', innerH).attr('fill', 'transparent').attr('stroke', 'rgba(255,255,255,0.08)').attr('stroke-width', 1).attr('rx', 6); chart.append('g').attr('class', 'cg-grid'); chart.append('g').attr('class', 'cg-axis cg-axis-x'); chart.append('g').attr('class', 'cg-axis cg-axis-y'); chart.append('g').attr('class', 'cg-plot'); chart.append('g').attr('class', 'cg-overlay'); const x = d3.scaleLinear().domain([xMin, xMax]).range([0, innerW]); const y = d3.scaleLinear().domain([yMin, yMax]).range([innerH, 0]); const xAxis = d3.axisBottom(x).ticks(8).tickSizeOuter(0); const yAxis = d3.axisLeft(y).ticks(8).tickSizeOuter(0); const gx = chart.select('.cg-axis-x').attr('transform', `translate(0,${(yMin <= 0 && yMax >= 0) ? y(0) : innerH})`).call(xAxis); const gy = chart.select('.cg-axis-y').attr('transform', `translate(${(xMin <= 0 && xMax >= 0) ? x(0) : 0},0)`).call(yAxis); gx.selectAll('path,line').attr('stroke', colors.axis || '#4f46e5').attr('stroke-width', 1.7); gy.selectAll('path,line').attr('stroke', colors.axis || '#4f46e5').attr('stroke-width', 1.7); gx.selectAll('text').attr('fill', colors.tick || 'rgba(226, 232, 240, 0.7)').style('font-size', '11px').style('font-family', '\"Inter\", sans-serif'); gy.selectAll('text').attr('fill', colors.tick || 'rgba(226, 232, 240, 0.7)').style('font-size', '11px').style('font-family', '\"Inter\", sans-serif'); if (!(args && args.grid === false)) { const gxGrid = d3.axisBottom(x).ticks(8).tickSize(-innerH).tickFormat(''); const gyGrid = d3.axisLeft(y).ticks(8).tickSize(-innerW).tickFormat(''); chart.select('.cg-grid').append('g').attr('class', 'cg-grid-x').attr('transform', `translate(0,${innerH})`).call(gxGrid); chart.select('.cg-grid').append('g').attr('class', 'cg-grid-y').call(gyGrid); chart.selectAll('.cg-grid line').attr('stroke', colors.grid || 'rgba(255,255,255,0.08)').attr('stroke-width', 1); chart.selectAll('.cg-grid path').attr('stroke', 'none'); } if (args && args.title) { root.append('text').attr('class', 'cg-title').attr('x', width / 2).attr('y', Math.max(18, margin.top - 18)).attr('text-anchor', 'middle').attr('fill', colors.label || 'rgba(226, 232, 240, 0.85)').style('font-family', '\"Inter\", sans-serif').style('font-weight', '700').style('font-size', '14px').text(String(args.title)); } if (args && args.xLabel) { root.append('text').attr('class', 'cg-xlabel').attr('x', margin.left + innerW / 2).attr('y', height - 16).attr('text-anchor', 'middle').attr('fill', colors.label || 'rgba(226, 232, 240, 0.85)').style('font-family', '\"Inter\", sans-serif').style('font-weight', '500').style('font-size', '12px').text(String(args.xLabel)); } if (args && args.yLabel) { root.append('text').attr('class', 'cg-ylabel').attr('x', 18).attr('y', margin.top + innerH / 2).attr('text-anchor', 'middle').attr('transform', `rotate(-90,18,${margin.top + innerH / 2})`).attr('fill', colors.label || 'rgba(226, 232, 240, 0.85)').style('font-family', '\"Inter\", sans-serif').style('font-weight', '500').style('font-size', '12px').text(String(args.yLabel)); } if (Array.isArray(args && args.points) && args.points.length) { const plot = chart.select('.cg-plot'); const points = args.points.filter(p => p && Number.isFinite(Number(p.x)) && Number.isFinite(Number(p.y))); const pts = plot.selectAll('.cg-point').data(points, d => d.id || `${d.x},${d.y}`).enter().append('circle').attr('class', d => `cg-point${d.id ? ` cg-point-id-${String(d.id).replace(/[^a-zA-Z0-9_-]/g, '-')}` : ''}`).attr('data-id', d => d.id !== undefined ? String(d.id) : '').attr('data-x', d => Number(d.x)).attr('data-y', d => Number(d.y)).attr('cx', d => x(Number(d.x))).attr('cy', d => y(Number(d.y))).attr('r', 0).attr('fill', colors.point || '#3b82f6').attr('stroke', colors.pointStroke || 'rgba(255,255,255,0.12)').attr('stroke-width', 2); pts.transition().duration(320).attr('r', 4.5); }"
    },
    "plotPoints": {
      "params": [
        "points",
        "radius",
        "color"
      ],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const chart = root.select('.cg-chart'); const plot = chart.select('.cg-plot'); if (plot.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const m = cfg.margin || {}; const width = Number(cfg.width) || 760; const height = Number(cfg.height) || 420; const margin = { top: Number(m.top) || 44, right: Number(m.right) || 34, bottom: Number(m.bottom) || 56, left: Number(m.left) || 64 }; const innerW = Math.max(40, width - margin.left - margin.right); const innerH = Math.max(40, height - margin.top - margin.bottom); const xMin = Number(svg.attr('data-cg-xmin')); const xMax = Number(svg.attr('data-cg-xmax')); const yMin = Number(svg.attr('data-cg-ymin')); const yMax = Number(svg.attr('data-cg-ymax')); if (![xMin, xMax, yMin, yMax].every(Number.isFinite)) return; const x = d3.scaleLinear().domain([xMin, xMax]).range([0, innerW]); const y = d3.scaleLinear().domain([yMin, yMax]).range([innerH, 0]); const raw = Array.isArray(args && args.points) ? args.points : []; const points = raw.filter(p => p && Number.isFinite(Number(p.x)) && Number.isFinite(Number(p.y))); if (!points.length) return; const r = Number(args && args.radius); const radius = Number.isFinite(r) ? Math.max(1, r) : 4.5; const fill = (args && args.color) || colors.point || '#3b82f6'; const sel = plot.selectAll(null).data(points).enter().append('circle').attr('class', d => `cg-point${d.id ? ` cg-point-id-${String(d.id).replace(/[^a-zA-Z0-9_-]/g, '-')}` : ''}`).attr('data-id', d => d.id !== undefined ? String(d.id) : '').attr('data-x', d => Number(d.x)).attr('data-y', d => Number(d.y)).attr('cx', d => x(Number(d.x))).attr('cy', d => y(Number(d.y))).attr('r', 0).attr('fill', fill).attr('stroke', colors.pointStroke || 'rgba(255,255,255,0.12)').attr('stroke-width', 2); sel.transition().duration(300).attr('r', radius);"
    },
    "connectPoints": {
      "params": [
        "ids",
        "points",
        "color",
        "width"
      ],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const chart = root.select('.cg-chart'); const plot = chart.select('.cg-plot'); if (plot.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const m = cfg.margin || {}; const widthCfg = Number(cfg.width) || 760; const heightCfg = Number(cfg.height) || 420; const margin = { top: Number(m.top) || 44, right: Number(m.right) || 34, bottom: Number(m.bottom) || 56, left: Number(m.left) || 64 }; const innerW = Math.max(40, widthCfg - margin.left - margin.right); const innerH = Math.max(40, heightCfg - margin.top - margin.bottom); const xMin = Number(svg.attr('data-cg-xmin')); const xMax = Number(svg.attr('data-cg-xmax')); const yMin = Number(svg.attr('data-cg-ymin')); const yMax = Number(svg.attr('data-cg-ymax')); if (![xMin, xMax, yMin, yMax].every(Number.isFinite)) return; const x = d3.scaleLinear().domain([xMin, xMax]).range([0, innerW]); const y = d3.scaleLinear().domain([yMin, yMax]).range([innerH, 0]); let pts = []; if (Array.isArray(args && args.ids) && args.ids.length) { for (const id of args.ids) { const el = plot.select(`.cg-point[data-id='${String(id).replace(/'/g, \"\\\\'\")}']`); if (el.empty()) continue; const px = Number(el.attr('data-x')); const py = Number(el.attr('data-y')); if (Number.isFinite(px) && Number.isFinite(py)) pts.push({ x: px, y: py }); } } else if (Array.isArray(args && args.points)) { pts = args.points.filter(p => p && Number.isFinite(Number(p.x)) && Number.isFinite(Number(p.y))).map(p => ({ x: Number(p.x), y: Number(p.y) })); } if (pts.length < 2) return; const lineGen = d3.line().x(d => x(Number(d.x))).y(d => y(Number(d.y))); const pathData = lineGen(pts); if (!pathData) return; const sw = Number(args && args.width); const strokeW = Number.isFinite(sw) ? Math.max(1, sw) : 2.5; const stroke = (args && args.color) || colors.line || '#10b981'; plot.append('path').attr('class', 'cg-conn').attr('data-points', JSON.stringify(pts)).attr('fill', 'none').attr('stroke', stroke).attr('stroke-width', strokeW).attr('stroke-linecap', 'round').attr('stroke-linejoin', 'round').attr('d', pathData).style('opacity', 0).transition().duration(320).style('opacity', 1);"
    },
    "drawFunction": {
      "params": [
        "expression",
        "xStart",
        "xEnd",
        "samples",
        "color",
        "width"
      ],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const chart = root.select('.cg-chart'); const plot = chart.select('.cg-plot'); if (plot.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const m = cfg.margin || {}; const widthCfg = Number(cfg.width) || 760; const heightCfg = Number(cfg.height) || 420; const margin = { top: Number(m.top) || 44, right: Number(m.right) || 34, bottom: Number(m.bottom) || 56, left: Number(m.left) || 64 }; const innerW = Math.max(40, widthCfg - margin.left - margin.right); const innerH = Math.max(40, heightCfg - margin.top - margin.bottom); const xMin = Number(svg.attr('data-cg-xmin')); const xMax = Number(svg.attr('data-cg-xmax')); const yMin = Number(svg.attr('data-cg-ymin')); const yMax = Number(svg.attr('data-cg-ymax')); if (![xMin, xMax, yMin, yMax].every(Number.isFinite)) return; const expr = args && typeof args.expression === 'string' ? args.expression.trim() : ''; if (!expr) return; const x = d3.scaleLinear().domain([xMin, xMax]).range([0, innerW]); const y = d3.scaleLinear().domain([yMin, yMax]).range([innerH, 0]); const xStartRaw = Number(args && args.xStart); const xEndRaw = Number(args && args.xEnd); const xStart = Number.isFinite(xStartRaw) ? xStartRaw : xMin; const xEnd = Number.isFinite(xEndRaw) ? xEndRaw : xMax; if (xEnd <= xStart) return; const sampleRaw = Number(args && args.samples); const samples = Number.isFinite(sampleRaw) ? Math.max(8, Math.min(800, Math.round(sampleRaw))) : 180; let fn; try { const body = `with (Math) { return (${expr.replace(/\\^/g, '**')}); }`; fn = new Function('x', body); } catch { return; } const pts = []; for (let i = 0; i <= samples; i++) { const xv = xStart + (i / samples) * (xEnd - xStart); let yv = null; try { yv = fn(xv); } catch { yv = null; } if (Number.isFinite(Number(yv))) pts.push({ x: xv, y: Number(yv) }); } if (pts.length < 2) return; const lineGen = d3.line().defined(d => Number.isFinite(d.y)).x(d => x(Number(d.x))).y(d => y(Number(d.y))); const dPath = lineGen(pts); if (!dPath) return; const stroke = (args && args.color) || colors.curve || '#eab308'; const swRaw = Number(args && args.width); const sw = Number.isFinite(swRaw) ? Math.max(1, swRaw) : 2.2; plot.append('path').attr('class', 'cg-func').attr('data-expression', expr).attr('data-x-start', xStart).attr('data-x-end', xEnd).attr('data-samples', samples).attr('fill', 'none').attr('stroke', stroke).attr('stroke-width', sw).attr('stroke-linecap', 'round').attr('stroke-linejoin', 'round').attr('d', dPath).style('opacity', 0).transition().duration(320).style('opacity', 1);"
    },
    "shadeEnclosedArea": {
      "params": [
        "expression",
        "xStart",
        "xEnd",
        "samples",
        "baseline",
        "color",
        "opacity"
      ],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const chart = root.select('.cg-chart'); const plot = chart.select('.cg-plot'); if (plot.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const m = cfg.margin || {}; const widthCfg = Number(cfg.width) || 760; const heightCfg = Number(cfg.height) || 420; const margin = { top: Number(m.top) || 44, right: Number(m.right) || 34, bottom: Number(m.bottom) || 56, left: Number(m.left) || 64 }; const innerW = Math.max(40, widthCfg - margin.left - margin.right); const innerH = Math.max(40, heightCfg - margin.top - margin.bottom); const xMin = Number(svg.attr('data-cg-xmin')); const xMax = Number(svg.attr('data-cg-xmax')); const yMin = Number(svg.attr('data-cg-ymin')); const yMax = Number(svg.attr('data-cg-ymax')); if (![xMin, xMax, yMin, yMax].every(Number.isFinite)) return; const expr = args && typeof args.expression === 'string' ? args.expression.trim() : ''; if (!expr) return; const xStartRaw = Number(args && args.xStart); const xEndRaw = Number(args && args.xEnd); const xStart = Number.isFinite(xStartRaw) ? xStartRaw : xMin; const xEnd = Number.isFinite(xEndRaw) ? xEndRaw : xMax; if (xEnd <= xStart) return; const sampleRaw = Number(args && args.samples); const samples = Number.isFinite(sampleRaw) ? Math.max(8, Math.min(800, Math.round(sampleRaw))) : 220; const baselineRaw = Number(args && args.baseline); const baseline = Number.isFinite(baselineRaw) ? baselineRaw : 0; let fn; try { const body = `with (Math) { return (${expr.replace(/\\^/g, '**')}); }`; fn = new Function('x', body); } catch { return; } const x = d3.scaleLinear().domain([xMin, xMax]).range([0, innerW]); const y = d3.scaleLinear().domain([yMin, yMax]).range([innerH, 0]); const pts = []; for (let i = 0; i <= samples; i++) { const xv = xStart + (i / samples) * (xEnd - xStart); let yv = null; try { yv = fn(xv); } catch { yv = null; } if (Number.isFinite(Number(yv))) pts.push({ x: xv, y: Number(yv) }); } if (pts.length < 2) return; const area = d3.area().x(d => x(Number(d.x))).y0(y(baseline)).y1(d => y(Number(d.y))); const dPath = area(pts); if (!dPath) return; const fill = (args && args.color) || colors.curve || '#eab308'; const opRaw = Number(args && args.opacity); const opacity = Number.isFinite(opRaw) ? Math.max(0.05, Math.min(0.9, opRaw)) : 0.22; plot.append('path').attr('class', 'cg-area').attr('data-expression', expr).attr('data-x-start', xStart).attr('data-x-end', xEnd).attr('data-samples', samples).attr('data-baseline', baseline).attr('fill', fill).attr('stroke', 'none').attr('d', dPath).style('opacity', 0).transition().duration(320).style('opacity', opacity);"
    },
    "highlightPoint": {
      "params": [
        "id",
        "x",
        "y",
        "active"
      ],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const plot = root.select('.cg-plot'); if (plot.empty()) return; let target = d3.select(null); if (args && args.id !== undefined && args.id !== null) { target = plot.select(`.cg-point[data-id='${String(args.id).replace(/'/g, \"\\\\'\")}']`); } if (target.empty() && Number.isFinite(Number(args && args.x)) && Number.isFinite(Number(args && args.y))) { const tx = Number(args.x); const ty = Number(args.y); const pts = plot.selectAll('.cg-point'); let best = null; let dist = Infinity; pts.each(function() { const el = d3.select(this); const px = Number(el.attr('data-x')); const py = Number(el.attr('data-y')); if (!Number.isFinite(px) || !Number.isFinite(py)) return; const d = (px - tx) * (px - tx) + (py - ty) * (py - ty); if (d < dist) { dist = d; best = this; } }); if (best) target = d3.select(best); } if (target.empty()) return; const active = !(args && args.active === false); target.interrupt().transition().duration(220).attr('stroke', active ? (colors.highlight || '#eab308') : (colors.pointStroke || 'rgba(255,255,255,0.12)')).attr('stroke-width', active ? 3 : 2).attr('r', active ? 6.5 : 4.5).style('filter', active ? 'url(#cg-glow)' : 'none').attr('data-highlight', active ? '1' : '0');"
    },
    "setViewport": {
      "params": [
        "xMin",
        "xMax",
        "yMin",
        "yMax",
        "animate"
      ],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const chart = root.select('.cg-chart'); if (chart.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const m = cfg.margin || {}; const widthCfg = Number(cfg.width) || 760; const heightCfg = Number(cfg.height) || 420; const margin = { top: Number(m.top) || 44, right: Number(m.right) || 34, bottom: Number(m.bottom) || 56, left: Number(m.left) || 64 }; const innerW = Math.max(40, widthCfg - margin.left - margin.right); const innerH = Math.max(40, heightCfg - margin.top - margin.bottom); let xMin = Number(args && args.xMin); let xMax = Number(args && args.xMax); let yMin = Number(args && args.yMin); let yMax = Number(args && args.yMax); if (!Number.isFinite(xMin)) xMin = Number(svg.attr('data-cg-xmin')); if (!Number.isFinite(xMax)) xMax = Number(svg.attr('data-cg-xmax')); if (!Number.isFinite(yMin)) yMin = Number(svg.attr('data-cg-ymin')); if (!Number.isFinite(yMax)) yMax = Number(svg.attr('data-cg-ymax')); if (![xMin, xMax, yMin, yMax].every(Number.isFinite)) return; if (xMax <= xMin) xMax = xMin + 1; if (yMax <= yMin) yMax = yMin + 1; svg.attr('data-cg-xmin', xMin).attr('data-cg-xmax', xMax).attr('data-cg-ymin', yMin).attr('data-cg-ymax', yMax); const x = d3.scaleLinear().domain([xMin, xMax]).range([0, innerW]); const y = d3.scaleLinear().domain([yMin, yMax]).range([innerH, 0]); const dur = args && args.animate === false ? 0 : 380; const xAxis = d3.axisBottom(x).ticks(8).tickSizeOuter(0); const yAxis = d3.axisLeft(y).ticks(8).tickSizeOuter(0); chart.select('.cg-axis-x').transition().duration(dur).attr('transform', `translate(0,${(yMin <= 0 && yMax >= 0) ? y(0) : innerH})`).call(xAxis).selection().selectAll('path,line').attr('stroke', colors.axis || '#4f46e5').attr('stroke-width', 1.7); chart.select('.cg-axis-y').transition().duration(dur).attr('transform', `translate(${(xMin <= 0 && xMax >= 0) ? x(0) : 0},0)`).call(yAxis).selection().selectAll('path,line').attr('stroke', colors.axis || '#4f46e5').attr('stroke-width', 1.7); chart.selectAll('.cg-axis text').attr('fill', colors.tick || 'rgba(226, 232, 240, 0.7)').style('font-size', '11px').style('font-family', '\"Inter\", sans-serif'); const gridOn = svg.attr('data-cg-grid') !== '0'; chart.select('.cg-grid').selectAll('*').remove(); if (gridOn) { const gxGrid = d3.axisBottom(x).ticks(8).tickSize(-innerH).tickFormat(''); const gyGrid = d3.axisLeft(y).ticks(8).tickSize(-innerW).tickFormat(''); chart.select('.cg-grid').append('g').attr('class', 'cg-grid-x').attr('transform', `translate(0,${innerH})`).call(gxGrid); chart.select('.cg-grid').append('g').attr('class', 'cg-grid-y').call(gyGrid); chart.selectAll('.cg-grid line').attr('stroke', colors.grid || 'rgba(255,255,255,0.08)').attr('stroke-width', 1); chart.selectAll('.cg-grid path').attr('stroke', 'none'); } chart.selectAll('.cg-point').transition().duration(dur).attr('cx', function() { return x(Number(d3.select(this).attr('data-x'))); }).attr('cy', function() { return y(Number(d3.select(this).attr('data-y'))); }); chart.selectAll('.cg-conn').transition().duration(dur).attr('d', function() { let pts = []; try { pts = JSON.parse(d3.select(this).attr('data-points') || '[]'); } catch { pts = []; } if (!Array.isArray(pts) || pts.length < 2) return d3.select(this).attr('d'); const lineGen = d3.line().x(d => x(Number(d.x))).y(d => y(Number(d.y))); return lineGen(pts) || d3.select(this).attr('d'); }); chart.selectAll('.cg-func').transition().duration(dur).attr('d', function() { const expr = d3.select(this).attr('data-expression') || ''; let xStart = Number(d3.select(this).attr('data-x-start')); let xEnd = Number(d3.select(this).attr('data-x-end')); let samples = Number(d3.select(this).attr('data-samples')); if (!Number.isFinite(xStart)) xStart = xMin; if (!Number.isFinite(xEnd)) xEnd = xMax; if (!Number.isFinite(samples)) samples = 180; if (xEnd <= xStart || !expr) return d3.select(this).attr('d'); let fn; try { fn = new Function('x', `with (Math) { return (${expr.replace(/\\^/g, '**')}); }`); } catch { return d3.select(this).attr('d'); } const pts = []; for (let i = 0; i <= samples; i++) { const xv = xStart + (i / samples) * (xEnd - xStart); let yv = null; try { yv = fn(xv); } catch { yv = null; } if (Number.isFinite(Number(yv))) pts.push({ x: xv, y: Number(yv) }); } if (pts.length < 2) return d3.select(this).attr('d'); const lineGen = d3.line().x(d => x(Number(d.x))).y(d => y(Number(d.y))); return lineGen(pts) || d3.select(this).attr('d'); }); chart.selectAll('.cg-area').transition().duration(dur).attr('d', function() { const expr = d3.select(this).attr('data-expression') || ''; let xStart = Number(d3.select(this).attr('data-x-start')); let xEnd = Number(d3.select(this).attr('data-x-end')); let samples = Number(d3.select(this).attr('data-samples')); let baseline = Number(d3.select(this).attr('data-baseline')); if (!Number.isFinite(xStart)) xStart = xMin; if (!Number.isFinite(xEnd)) xEnd = xMax; if (!Number.isFinite(samples)) samples = 220; if (!Number.isFinite(baseline)) baseline = 0; if (xEnd <= xStart || !expr) return d3.select(this).attr('d'); let fn; try { fn = new Function('x', `with (Math) { return (${expr.replace(/\\^/g, '**')}); }`); } catch { return d3.select(this).attr('d'); } const pts = []; for (let i = 0; i <= samples; i++) { const xv = xStart + (i / samples) * (xEnd - xStart); let yv = null; try { yv = fn(xv); } catch { yv = null; } if (Number.isFinite(Number(yv))) pts.push({ x: xv, y: Number(yv) }); } if (pts.length < 2) return d3.select(this).attr('d'); const area = d3.area().x(d => x(Number(d.x))).y0(y(baseline)).y1(d => y(Number(d.y))); return area(pts) || d3.select(this).attr('d'); }); root.selectAll('.cg-annotation').remove();"
    },
    "annotatePoint": {
      "params": [
        "id",
        "x",
        "y",
        "text"
      ],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; root.selectAll('.cg-annotation').remove(); if (!args || !args.text) return; const plot = root.select('.cg-plot'); if (plot.empty()) return; let target = d3.select(null); if (args.id !== undefined && args.id !== null) { target = plot.select(`.cg-point[data-id='${String(args.id).replace(/'/g, \"\\\\'\")}']`); } if (target.empty() && Number.isFinite(Number(args.x)) && Number.isFinite(Number(args.y))) { const tx = Number(args.x); const ty = Number(args.y); let best = null; let dist = Infinity; plot.selectAll('.cg-point').each(function() { const el = d3.select(this); const px = Number(el.attr('data-x')); const py = Number(el.attr('data-y')); if (!Number.isFinite(px) || !Number.isFinite(py)) return; const d = (px - tx) * (px - tx) + (py - ty) * (py - ty); if (d < dist) { dist = d; best = this; } }); if (best) target = d3.select(best); } if (target.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; const cx = Number(target.attr('cx')); const cy = Number(target.attr('cy')); if (!Number.isFinite(cx) || !Number.isFinite(cy)) return; const g = plot.append('g').attr('class', 'cg-annotation').style('opacity', 0); g.append('text').attr('x', cx + 10).attr('y', cy - 10).attr('text-anchor', 'start').attr('fill', colors.annotation || '#c7d2fe').style('font-family', 'ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace').style('font-size', '12px').style('font-weight', '500').style('filter', 'drop-shadow(0 2px 4px rgba(0,0,0,0.5))').text(String(args.text)); g.transition().duration(300).style('opacity', 1).attr('transform', 'translate(0, -4)');"
    },
    "clearHighlights": {
      "params": [],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const cfg = registry && registry.config ? registry.config : {}; const colors = cfg.colors || {}; root.selectAll('.cg-point').interrupt().transition().duration(220).attr('stroke', colors.pointStroke || 'rgba(255,255,255,0.12)').attr('stroke-width', 2).attr('r', 4.5).style('filter', 'none').attr('data-highlight', '0'); root.selectAll('.cg-annotation').interrupt().transition().duration(160).style('opacity', 0).remove();"
    },
    "clearPlot": {
      "params": [],
      "code": "const root = svg.select('.cg-root'); if (root.empty()) return; const plot = root.select('.cg-plot'); if (plot.empty()) return; plot.selectAll('.cg-point, .cg-conn, .cg-func, .cg-area, .cg-annotation').remove();"
    }
  }
}
